# α 人资管理入口一致性收口日志

## 本轮范围

仅调整 α 原型，覆盖 `/employees`、`/employees/attendance`、`/employees/performance` 与 `/employees/level-rates`，不修改 β Web、API 或数据协议。审查确认旧 `/hr/expenses` 已重定向到 `/finance/expenses`，因此不继续维护平行的人资费用页面。

## 完成内容

- 员工列表接入 `PageShell`、20px `PageHeader`、`ProcessMetricGrid` 与 `FilterBar`，保留一级页面不显示面包屑的规则。
- 考勤、绩效、职位管理统一增加“员工管理 / 当前页面”二级面包屑，模块标题统一为 16px。
- 四页统一四项业务摘要；员工、考勤和绩效补齐筛选结果、重置逻辑与空态。
- 员工查看、编辑、更多操作，考勤批准/拒绝及费率编辑统一为带 Tooltip 和业务对象名称的图标操作。
- 职位页补充当前职位、职级数与平均时薪摘要；职位页签支持横向滚动，职级表单在窄屏切换单列。
- 考勤类型颜色归并到主题语义变量，不再保留局部硬编码色板。

## 验证结果

- 定向 Vitest：4 项人资页面结构回归全部通过。
- 全量 Vitest：88 个测试文件、985 项测试全部通过。
- α 构建通过，仅保留仓库既有的 Gantt 注释与大 chunk 提示。
- 浏览器验收覆盖员工搜索/重置与新增弹窗、考勤/绩效新增弹窗、费率行内编辑及新增职级弹窗。
- 1200px 与 760px 下四页均无页面级横向溢出；窄屏指标两列、筛选铺满、页头纵向排列，控制台无错误事件。

## 后续工作

继续检查 α 系统配置域：组织架构、用户权限、公司主体与系统通用配置，重点统一二级面包屑、页面标题、配置分组、危险操作层级和窄屏表单布局。

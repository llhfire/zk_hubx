# 2026-08-28 α 二级页面面包屑首批收口日志

## 本次范围

仅调整 α 版二级页面层级表达、出差申请首帧错误和相关说明文档，未修改 β Service、Workers 或 D1 链路。

## 已完成

- 合同新建、编辑和预览统一为“合同管理 → 合同列表 → 当前合同/操作”面包屑；错误态保留可返回合同列表的层级。
- 员工详情统一为“员工管理 → 员工列表 → 员工姓名”，移除与面包屑重复的顶部返回按钮。
- 员工日报视图、项目日报明细和岗位工作配置统一为“日报 → 日报列表 → 当前页面”。
- 新建出差申请统一为“差旅管理 → 出差申请 → 新建出差申请”，并复用 20px `PageHeader` 展示范围说明。
- 所有页面复用 `PageShell`/`PageBreadcrumb`，祖先节点可点击，当前节点仅展示文本。
- 修复 `TripForm` 出差目的输入在渲染阶段引用未定义 `value`、导致 `/travel/trips/new` 整页崩溃的问题。

## α 边界

本次未重构页面业务结构、数据源或提交流程。合同预览仍需从合同编辑流程携带当前草稿状态；直接访问无上下文链接时继续展示合同不存在错误态。

## 验证

- 新增 `TripForm` 首帧回归测试，覆盖统一面包屑、20px 页头说明和出差目的输入渲染。
- 1440×1000 浏览器逐页检查 8 个目标页面，面包屑标签、祖先链接和内容起点正确，均无页面级横向溢出。
- 760×900 检查新建出差申请，面包屑与 20px 页头正常，页面无横向溢出。
- 全量 Vitest 通过：84 个测试文件、973 项测试全部通过；α 生产构建通过。

## 后续

继续检查审批模板、业务映射等剩余嵌套页面的层级语义，并收口系统日志、意见反馈等剩余系统列表。

# 下班交接

> 更新时间：2026-08-30（项目详情与合同回款完成，前端分包发布恢复）

## 本次进度

### 项目详情 360 信息优先级与交付阶段

- 按 `grill-with-docs` 完成逐项决策并实施；保持既有顶部、70:30 主侧栏和双 Tab 总体布局。
- 顶部收口为进度、工期、工时、回款、缺陷、风险/阻塞六项执行指标，`main`、`side`、`major` 状态写入 URL。
- 交付阶段改为可点击轻胶囊：合同签订按付款条件动态出现，实施过程聚焦方案设计、开发、测试、验收、回款结项；命中付款节点时显示人民币标识，完成阶段仍可打开查看历史。
- 项目进度压缩为两行高度；基础信息改为字段名与字段值横排、三列疏密结合，长字段独占整行；业务线旁标签缩短为「外包」。
- 项目动态改为只读聚合，不提供快速记录，大事记默认开启；最新进展标题左对齐，右侧跟进页签不再显示数字提醒。
- 项目动态依 ADR-0097 投影合同、回款、确认书、会议、任务、关键跟进与风险，支持日期分组、加载更多、结构化事实与来源钻取。

### 帕奇宠C端一期完整演示数据

- 以 `prod-112` 为项目基准补齐项目、阶段、任务、质量、风险和客户信息，作为项目详情演示夹具。
- 主合同金额 100,000 元：已回款 90,000 元，待回款 10,000 元。
- 补充合同一「体验增强」金额 28,000 元，两期均已回款；补充合同二「AI陪伴能力接入」金额 16,000 元，已回款 8,000 元、待回款 8,000 元。
- 两份补充合同均补齐来源报价、合同编号、版本、审批、扫描件、付款节点、实收记录与催收信息。
- 项目有效合同额 144,000 元，累计实收 126,000 元，待收 18,000 元；合同、回款和交付阶段人民币标识使用同一事实源。

### 线索、客户与生产项目数据

- 从旧版生产站完成 36 条项目快照迁移并置顶接入列表和 360 详情，保留接口原始字段和空值语义。
- 保留 8 条跨模块兼容夹具，保证合同、回款、日报与已成交线索关联不被破坏。
- 小批量线索与客户演示数据已脱敏替换；列表、详情、流转、成交合同和项目关联口径已对齐。

### 文档与验证

- CONTEXT、ADR-0097、PRD、实现计划、功能看板、功能架构图和工作记录已随实现对齐。
- β 接线、D1、Workers、系统洞与施工阶段均未变化，`HubX/docs/ZK-HubX技术架构.html` 无需修改。
- `npm run build` 通过；全量 Vitest 共 96 个测试文件、1025 项测试全部通过。
- 测试仍有 Arco/Router 的 `useLayoutEffect` 服务端渲染提示，不影响测试结果。

### 构建分包与发布恢复

- 将 87 个同步页面入口改为路由级懒加载，页面加载期间由主布局内的轻量加载态承接，不改变既有路由与页面布局。
- α / β 共用 `apps/viteBuildChunks.ts`，稳定拆分 React、Arco、图标、图表、编辑器、甘特图、拖拽与日期依赖，避免两套 Vite 配置漂移。
- 最大入口 JS 由约 3.93 MB 降至 α 548 KB / β 559 KB；其余最大文件为 ExcelJS 940 KB、Arco 790 KB、全局 CSS 615 KB，均低于 1 MB。
- 分包后 α 上传 154/154、β 上传 153/153 成功；部署列表已核对两端 Source 均为分包功能提交 `8db3c19`。

## 当前状态

| 项目 | 状态 |
|---|---|
| 旧版项目迁移 | 36 条全部完成，生产项目置顶接入列表与 360 详情 |
| 项目详情信息优先级 | α 设计、编码、文档和验证完成；共享 UI 同步进入 β 构建 |
| 帕奇宠演示链路 | 项目、主合同、两份补充合同、报价与回款数据完整 |
| 线索/客户演示数据 | 已脱敏替换并完成跨模块关联回归 |
| Git | 分包功能提交 `8db3c19` 已推送至 `origin/main`；本交接随后仅补充发布记录 |
| Cloudflare | α 部署 `66e6376b`、β 部署 `2eaf4b6a` 均成功，Source 同为 `8db3c19` |
| Workers / D1 | `apps/api`、schema 与 migrations 无改动，不发布 Workers、不执行迁移 |

## 下次待办

1. 继续核对生产项目详情是否需把关联人员、最后跟进时间等原始字段扩展为专门的可视区域；原始值已保留在快照中。
2. 决定演示结束后是否继续保留 8 条兼容夹具，或将合同、回款、日报事实源全部迁到生产项目后再删除。
3. 继续验证帕奇宠两份补充合同的付款节点在合同页、回款页和交付阶段历史中的一致性。
4. 原有 β 待办继续：真实数据阶段 3–4、B5 线上冒烟、U1 洽谈项目生成与其他域接缝。

## 踩坑与约束

- 生产项目接口允许优先级、业务线、起止日期、预算工时为空；迁移时必须保留空值语义。
- 不能直接删除旧项目夹具：合同、回款、日报和成交线索仍依赖部分本地项目 id。
- 补充合同必须通过 `kind: supplement` 与 `parentContractId` 挂在主合同下；有效合同额和回款汇总不得再维护第二套手工总数。
- 阶段人民币标识来自合同付款计划的触发条件；合同签订只有命中付款条件时才作为第一步出现。
- 项目备注可能包含内部原型链接或手机号，写入 mock 前需脱敏；访问令牌只允许在浏览器会话内使用。
- 项目列表成为共享事实源后会影响日报工作归属和成交线索读模型，迁移时必须同步跑跨模块回归。
- Wrangler 默认并发上传最后三项大资源时可能长期停滞；本次通过路由懒加载与公共依赖分包把最大资源压到 1 MB 以下后恢复。以后仍以部署列表 Source 为成功口径，不能只看上传命令退出状态。

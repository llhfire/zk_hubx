# 下班交接

> 更新时间：2026-09-05（β 版接入 Supabase 数据库、生产数据全量接管、建线索与写跟进缺陷修复）

## 本次进度

- **β 版数据库升级为 Supabase (PostgreSQL，ADR-0106)**：
  - 创建 Supabase 9 张核心业务表（`quotes`, `contracts`, `leads`, `lead_followups`, `lead_transfers`, `projects`, `cases`, `collections`, `employees`），采用文档式 JSONB 存储与乐观锁。
  - 实现统一适配器架构（优先 Supabase，无缝兼容与自动回退 D1 / 内存存储）。
  - 实现 `HubX/scripts/db.js` CLI 管理工具，全权接管状态检查、数据查看、SQL 执行、批量导入及清空运维。
- **真实业务数据全量迁移与注入**：
  - 批量导入 100 名员工人事档案与技能信息。
  - 批量导入 54 个履约项目及回款账单。
  - 批量导入 2 份核心合同及关联信息。
  - 批量导入 1882 条历史全量线索，确保与历史 MySQL 资产 100% 对齐。
- **线索建立与跟进缺陷修复**：
  - **建线索 Bug**：修复 HTTP Header `X-Actor` 携中文触发浏览器 ByteString 校验抛错；后端 `POST /api/leads` 补齐 `createTime`/`updateTime` 字段，避免前端排序崩溃。
  - **写跟进 Bug**：
    - 「我的线索」（`MyLeads.tsx`）表格操作列补充「添加跟进」点击响应并挂载 `LeadFollowUpModal`。
    - 线索详情页（`LeadDetail360.tsx`）全面加固 `lead?.` 防空指针兜底与默认状态回退。
    - `leadService.ts` 补齐对非 2xx HTTP 状态码的错误捕获与提示。
    - 后端数据库适配器与接口全面支持 `L-` 前缀互转（`5942` ⇄ `L-5942`）、去池前缀及按 `key` 兜底定位，确保任意入口提交均能准确定位线索。
- **全量回归与线上部署**：
  - 全库 114 个测试文件、1097 个 Vitest 单元测试全部通过。
  - 完成 Cloudflare Workers 后端及 Pages β 版前端构建与全量部署，线上端到端交互闭环验证通过。

## 当前状态

| 项目 | 状态 |
|---|---|
| β 后端数据库 | Supabase (PostgreSQL) 托管运行，9 张表就绪，数据已全部导入 |
| 数据库 CLI | `node scripts/db.js` 全权接管状态查看、数据操作与 DDL 执行 |
| 线索全链路 | 建线索、查看详情、列表写跟进、详情写跟进端到端全部通畅 |
| 全量测试 | 114 passed / 114 files (1097 passed / 1097 tests) |
| 线上部署 | Workers API（`zkhubx-api`）与 Pages β 前端（`beta.zkhubx.com`）均已更新上线 |
| 本地开发 | α 版开发服务器正常运行在 `http://localhost:5173` |
| 工作记录 | 已追加 `workLog.config.json` 2026-09-05 记录 |
| 技术架构图 | 已在 `ZK-HubX技术架构.html` 明确标注 Supabase 架构与适配层 |

## 下次待办

1. **推进 P1 出差详情页（TripDetail.tsx）标杆化升级**：
   - 改造顶部微指标（差旅预算执行率、报销核销比、审批合规节点、关联项目成本沉淀）。
   - 将申请人、关联项目、关联客户、关联线索从纯文本升级为穿透高亮链接。
   - 改造主侧双栏工作区与 URL 驱动参数联动。
2. **推进 P1 合同成本详情页（ContractCostDetail.tsx / ContractDetail.tsx）** 标杆化升级。

## 踩坑与约束

- **HTTP Header 编码规范**：HTTP 标头规范仅允许 ByteString (ASCII) 字符，前端向服务端传递中文身份或备注（如 `X-Actor`）时，客户端必须显式 `encodeURIComponent`，服务端统一 `decodeURIComponent`，杜绝 Fetch 抛出字符集越界异常。
- **时间戳与排序安全性**：列表按时间降序排序时（如 `localeCompare`），必须防止字段为 `undefined` 或空值，统一做 `(b.createTime || '').localeCompare(a.createTime || '')` 容错。
- **主键别名与前缀容错**：线索在不同系统与交互中可能存在数字编号（`5942`）、标准主键（`L-5942`）或路由标识（`public-5942`），后端适配器必须具备规范化解析与候选查找能力，杜绝 404。

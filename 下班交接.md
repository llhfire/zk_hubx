# 下班交接

> 会话上下班交接文档（固定单份）。上班时先读此文件恢复上下文。
> **下班 = 写本文件 + 有代码改动才发布 Cloudflare**（用户说「下班」即触发，不要再确认）。发布步骤见 `HubX/docs/DEPLOYMENT.md`「下班发布」。
> 更新时间：2026-08-19（下班：运营费用 grill 收束 + 阶段 A 计划。`packages/ui` 看板有改，发 α/β；`apps/api` 无改，不发 Workers）

## 本次进度（本次会话做了什么）

**运营费用 grill 收束。PRD、ADR、阶段 A 文件级计划已齐。业务页面未改（没有新建 `operating-expense/`）。**

1. **grill**（策划案 v1.2 → 词表 / ADR `0076`–`0085`）：
   - 财务域「运营费用」，不是人资费用中枢，不算项目毛利。
   - 工资不进台账、不进池；看板按工资表当前金额平移。
   - 8 个可录入科目；`LABOR` 只作项目成本桶。
   - 确认会发生即入账（不等打款）。
   - 公摊：大小周工天 × 在职折算 × 8，只按全公司除。
   - 4 Tab；仅财务 / 人资行政可进。

2. **四件套**：
   - PRD：`文档/PRD/PRD-运营费用管理.md`
   - 计划：`计划/当前/operating-expense-dev-plan.md`（阶段 A 目录、纯函数、单测夹具、15 处改动）
   - 看板「运营费用」四项 **已设计**
   - 架构图财务域现有模块 4→5
   - 策划案修订在仓库外：`research/人资管理板块 · 运营费用管理重构策划案.md` v2.0（不进本 git）

## 当前状态要点

- 架构：单一 git 仓库；monorepo 在 `HubX/`
- 已通 HTTP：报价域、合同域；项目/商机/交付计划仍是内存 mock
- 部署：α / β 随本次提交发布；Workers 未动
- git：`main`
- 本地 α：`http://localhost:5173/`
- 下一刀编码：阶段 A，计划已展开

## 下次待办（按优先级）

1. **开工阶段 A**：按 `计划/当前/operating-expense-dev-plan.md` 建 `pages/operating-expense/`，替换 `/hr/expenses`，财务统计去掉运营费用录入。
2. 阶段 A 验收过后再做 B（差旅 / 投流 / 报销投影）。
3. 浏览器抽空看差旅、精益交付（上轮移植）。
4. U3（作废↔搁置）或其他报价阶段。
5. P0② 回款权限矩阵单测 / 基线债。

## 踩坑提醒

- **阶段 A 不要改** `getHourlyOpCost` / 项目成本页（阶段 D）。本模块用 `expenseCalc.hourlyOverheadRate`。
- **不要在 `HrExpenseManagement.tsx` 上继续长**：新目录 `pages/operating-expense/`。
- **工天阶段 A 用每月天数表**，不要先做完整大小周日期 UI。
- **Employee 没有 leaveDate**：阶段 A mock 补字段验证折算即可，不要顺手做员工档案。
- **单测在 `HubX/apps/prototype` 下跑**，从 HubX 根跑 vitest 会因路径别名失败。
- **下班发布三端清单制**：α 必发 / β 必发 / Workers 仅 `apps/api` 有改才发。核对 `wrangler pages deployment list` 的 Source SHA = HEAD。
- Workers 必须在 `HubX/apps/api` 下 `npx wrangler deploy`。

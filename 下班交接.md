# 下班交接

> 更新时间：2026-09-01（0829 回款看板导入、抽屉回显与付款甘特图逾期色阶）

## 本次进度

### 回款看板数据

- 新增 `recoveryBoardData.ts`，按 `文档/数据/0829项目回款看板.html` 导入 25 个项目的客户、销售、合同额、付款期次、已到账记录、预计日期和备注。
- 导入金额校验：合同总额 ¥2,838,200，已回款 ¥1,428,120。
- 合同服务与独立实收台账同步注入这批数据；回款主看板、行动队列和回款预测均优先展示 `recovery-board` 数据源，旧合同种子不再进入回款页。

### 回款详情与甘特图

- 修复右侧回款时间线按日期累计导致的期次错配：现在按实收记录 `period` 独立归属，避免中国铁建项目尾款被误判为已到账。
- 右侧抽屉新增卡点/催款后等待服务刷新，记录立即回显；卡点选择付款期次后自动带出该期计划金额，金额仍可调整。
- 甘特图逾期付款节点按 0829 数据截点显示五级警告色：橙色（1–7 天）、红色（8–14 天）、深红（15–30 天）、褐色（31–60 天）、黑色（60 天以上），并补充图例。

### 报价、合同与其他既有交付

- 报价文件/数据双轨、审批汇合、补充报价/补充合同、确认书生成与归档等既有改动继续保留。
- 合同回款看板视觉、合同在线查看抽屉、付款期次卡点关联等既有改动继续保留。

## 当前状态

| 项目 | 状态 |
|---|---|
| 0829 回款数据 | 25 个项目已导入，汇总金额与源文件一致 |
| 中国铁建项目 | 首付款 ¥11,500、二期款 ¥9,200 已按期次到账；尾款 ¥2,300 未到账 |
| 卡点/催款抽屉 | 新增后立即回显；卡点金额按付款期次自动带出 |
| 付款甘特图 | 逾期五级警告色及图例已完成 |
| 测试与构建 | 全量测试此前 1076 项通过；本次回款相关测试与构建通过 |
| Git / 发布 | 本次有代码改动，已准备按下班流程提交并发布 α/β |

## 下次待办

1. 用浏览器手走一遍中国铁建项目详情，确认期次金额、到账日期和尾款逾期状态视觉展示。
2. 验证卡点金额自动带出后手动调整、催款记录新增和解决卡点后的即时刷新。
3. 若接入真实接口，将 `recoveryBoardData.ts` 替换为服务端数据适配层，并保留期次明确关联约束。

## 踩坑与约束

- 实收金额必须按付款期次关联，不能按“到账日期早于计划日期”做累计判断，否则后续期次会被误判为已收。
- 0829 看板是快照数据；甘特图逾期色阶以 `2026-08-29` 为统一口径，避免浏览日期变化造成展示漂移。
- 回款页通过 `dataSource === 'recovery-board'` 过滤新数据；旧合同种子仍供其他合同域页面和既有测试使用。
- 卡点必须关联具体付款期次；新增卡点金额默认读取当前合同（含补充合同自身）的对应期次金额。

# 下班交接

> 更新时间：2026-08-31（项目详情、交付计划、回款与中科标准件适配收口）

## 本次进度

### 项目详情与交付计划

- 项目阶段条对涉及补充合同的项目增加独立「补充协议」步骤，位于回款结项之前；阶段弹层只保留付款节点、已完成和待完成事项，修复胶囊对齐、条目排版及容器溢出。
- 六项执行卡片改用业务对应图形：里程碑实心/虚线、工期时间轴、工时完成/预期/超出方块、合同回款金额进度条；项目进度卡可直接进入甘特图。
- 帕奇宠C端一期交付计划以标准 SOP 为骨架，根据合同标的选择板块，并将付款、交付和验收条件生成合同节点与依据快照。
- 交付计划页移除重复的步骤进度、指标卡和右侧动态栏，压缩页头并修复甘特图日期、行高和任务错位。

### 合同、回款与发票

- 项目合同信息默认折叠，仅显示编号、公司名称、签约主体和签约日期；补充查看、下载、展开入口。
- 新增实收可选择多个未收足期次，金额按选择顺序优先冲抵；未完全冲足显示「部分已收」，已收足期次从候选列表移除。
- 回款期次与实收、开票状态联动：有效发票显示「已开票」，冲红后取消；修复旧线索数据缺少集合字段导致实收弹窗 `map` 崩溃。

### 报价与合同标准件

- 以中科报价模板和合同模板完善签约主体路由、字段校验、正文条款、签章区和下载文件名；非中科软通主体不再误用软通正文。
- 报价功能清单按模块与明细行分摊技术成本，报价调整/补充影响独立列示，所有行金额严格合计到含税总价。
- 报价与合同下载由伪文本扩展名改为真实 OOXML DOCX；模板输入文件随仓库归档。

### 视觉与稳定性

- 线索/项目列表跟进图标与垃圾线索图标替换为指定 SVG；项目详情阶段条和信息卡完成密度与对齐收口。
- 移除 ReactQuill/Quill 1 漏洞链，改为动态加载的 Quill 2 适配组件；图片插入、只读详情和 Arco 表单回传接口保持不变。
- 工具链升级至 React Router 7.18.3、Vite 7.3.6、Vitest 4.1.11、Wrangler 4.127.1；npm audit 从 19 个漏洞降为 0。
- 构建继续保留路由懒加载和稳定供应商分块；ExcelJS 为按需导出块，已按实际 gzip 体积校准构建阈值。

### 验证与发布

- `npm test`：110 个测试文件、1072 项测试全部通过。
- α、β 生产构建通过；Workers 4 dry-run 通过并识别 D1 绑定；`npm audit` 为 0；`git diff --check` 通过。
- 浏览器实测线索详情、报价、合同向导、合同模板、项目详情和 390px 窄屏；Quill 编辑器挂载/变量同步正常，应用控制台无错误。
- `apps/api` 仅升级 Wrangler 与 Workers 类型包，API 源码、schema 和 migrations 未改；本次不执行 D1 迁移，但按规则发布 Workers 与 α/β Pages。

## 当前状态

| 项目 | 状态 |
|---|---|
| 项目详情阶段与六指标 | 已完成，补充协议与图形化指标已收口 |
| 帕奇宠交付计划 | 已按 SOP + 合同依据生成并完成甘特图校准 |
| 合同/回款/发票 | 多期冲抵、部分已收、开票红冲联动已完成 |
| 报价/合同标准件 | 中科多主体模板适配与真实 DOCX 下载已完成 |
| 工具链与依赖 | Vite 7 / Vitest 4 / Wrangler 4；审计 0 漏洞 |
| 测试 | 110 文件、1072 项全绿 |
| Workers / D1 | Workers 依赖升级需发布；D1 无迁移 |
| Git / Pages | 本次下班统一提交并发布 α、β，最终以 deployment list Source SHA 核对 |

## 下次待办

1. 实际演示帕奇宠项目详情、交付甘特图、合同折叠和多期实收，按业务反馈做小范围校准。
2. 补充真实文件下载的跨浏览器验收，重点检查 Word 中表格宽度、分页、签章区和中文字体回退。
3. 销售域 α 继续观察，不扩张独立待办、自动处罚或 AI 阶段决策；如进入 β，先建设服务端事实源、权限和审计。
4. 延续 β 主线：真实数据阶段 3–4、B5 线上冒烟、U1 洽谈项目生成与跨域刷新幂等。

## 踩坑与约束

- 交付计划不是固定 SOP 的副本：SOP 只提供骨架，合同标的、付款和交付/验收条件必须作为生成依据并保存快照。
- 补充协议步骤只在存在有效补充合同时出现，回款条件不得再混入前序交付步骤。
- 一笔实收可冲抵多期，但必须按用户选择顺序分摊；不能把未冲足期次误标为已收。
- 发票状态从有效开票记录投影，冲红后必须取消「已开票」，不得另存一套脱节布尔值。
- 报价行金额合计必须严格等于含税总价；模板字段缺失、付款比例不为 100% 均应阻断生成。
- 已发布合同模板不可原地覆盖；非软通主体不得回退使用软通 19 章正文。
- 共享 UI 包必须明确声明跨 workspace 运行时依赖，不能依靠 npm 偶然提升；依赖升级后需重启旧 Vite 进程。
- ExcelJS 虽为约 937 KB 原始块，但已通过动态导入仅在导出时下载；不要为了消除数字强拆出 Arco 循环 chunk。
- `文档/归档 2.zip` 是无关本次工作的本地归档，不进入提交。

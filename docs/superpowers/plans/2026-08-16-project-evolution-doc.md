# 《演进史.md》项目演进档案 — 实施计划

> **For agentic workers:** 本任务是**撰写一份 Markdown 文档**（非代码），不适用 TDD。按 Task 顺序执行，每 Task 产出文档的一个章节，最后组装全文并验证。
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在根目录产出《演进史.md》——记录 ZK HubX 从需求采集（5-6月）到当前（08-16）所有重要改变的演进档案（自用备忘）。

**Architecture:** 单份中文 Markdown，5 章结构（文档说明 / 阶段总览表 / 7 段分阶段详述 / 当前状态快照 / git 提交清单附录），每阶段统一五块模板（背景与触发 / 做了什么 / 关键决策 / 结构·数据变化 / 遗留与风险）。

**Tech Stack:** Markdown，中文，根目录项目文档族。

**设计文档:** `docs/superpowers/specs/2026-08-16-project-evolution-doc-design.md`

---

## 素材总账（所有章节共用）

| 阶段 | 主素材来源 | 性质 |
|---|---|---|
| 0 需求采集 | `OA需求文档.md`、`原始需求池.md`（2026-5-12/5-15/5-21）、`录音分析结果.md`、`软件外包项目交付执行 SOP 1.md` | 现有文档直读 |
| 1 立项入库 | git（`de28286`~`c03ecb7`）、`ZK-HubX-工作清单.md`、`实施计划.md`（α→β→v1.0）、`进度对齐列表.md`（里程碑时间线：α版启动 08-13） | git + 文档 |
| 2 核心模块 | `SYSTEM-OVERVIEW.md`（模块清单）+ 工作清单「已完成模块」表 + 记忆（08-13 工作清单对齐）+ 代码现状 | 逆向还原为主 |
| 3 报价重构 | 根 `PLAN.md`（报价重构决策）、工作清单 A2/报价文档（场景设计/流程规范/审查报告）、记忆（08-14 UI 优化） | 文档 + 记忆 |
| 4 四个工作台 | `packages/ui/src/app/pages/quotation/SESSION-LOG-2026-08-15.md`（端+平台/WASD/PDF）+ 记忆 08-15 | 会话日志最可靠 |
| 5 monorepo | git（`865ff80`）+ `HubX/docs/ALPHA-BETA-ARCHITECTURE.md` + 记忆 08-16 | git + 文档 |
| 6 部署+合同域 | git（`9eda9d0`~`6ebc5fa`）+ `HubX/docs/DEPLOYMENT.md` + 记忆 08-16 | git + 文档 |

---

### Task 1: 撰写「1. 文档说明」+「2. 阶段总览表」

**文件:** 创建 `演进史.md`（根目录），写前两章。

- [ ] **Step 1: 写文档说明**
  内容：用途（自用备忘/跨会话存档）、覆盖范围（需求采集 2026-05 至 08-16）、如何使用（阶段总览表定位 + 分阶段详读 + 快照看现状）、信息源与可信度说明（有据直写 / 据代码现状还原 / 待补充确认 三种标注含义）。

- [ ] **Step 2: 写阶段总览表**
  表格：阶段｜日期｜主题｜一句话。7 行（0 需求采集 5-6月 / 1 立项入库 08-13 / 2 核心模块 08-13→14 / 3 报价重构 08-14 / 4 四个工作台 08-15 / 5 monorepo+前后端 08-16 上午 / 6 部署+合同域 08-16 下午）。

- [ ] **Step 3: 提交**

```bash
git add "演进史.md"
git commit -m "docs: 演进史文档（文档说明+阶段总览）"
```

---

### Task 2: 撰写阶段 0（需求采集）与阶段 1（立项与代码入库）

**文件:** 修改 `演进史.md`，在总览表后插入两节。

- [ ] **Step 1: 读素材**
  读 `原始需求池.md` 前 60 行（三条需求批次 2026-5-12/5-15/5-21）、`OA需求文档.md` 前 40 行、`录音分析结果.md` 的「会议核心结论」段（前 40 行）、`软件外包项目交付执行 SOP 1.md` 的文档说明段、`实施计划.md` 的版本定义段、`进度对齐列表.md` 的里程碑时间线段。

- [ ] **Step 2: 写阶段 0（需求采集 5-6月）**
  五块：背景（中科集团内部管理数字化立项，多轮会议录音）、做了什么（OA-1.0 项目模块规划 / 原始需求池三批 / 录音分析四条核心结论 / 交付 SOP / PRD 文档族）、关键决策（不做 AI/不做对外售卖/只做内部管理）、结构数据变化（需求→PRD 文档族，清单见进度对齐列表第五节）、遗留（部分 PRD 仍 🔧）。

- [ ] **Step 3: 写阶段 1（立项与代码入库 08-13）**
  五块：背景（α版启动 08-13，实施计划三版本递进）、做了什么（first commit 系列 / 将 HubX 前端代码并入根仓库统一单一仓库 `c03ecb7` / 工作清单 08-13 对齐 / 进度对齐列表/表建立）、关键决策（α版原型先行、单一仓库统一管理、嵌套 git 合并为单仓库）、结构数据变化（`ZK-HubX-工作清单.md` 建立、根目录项目文档族成型）、遗留（文档清单不全待补）。

- [ ] **Step 4: 提交**

```bash
git add "演进史.md"
git commit -m "docs: 演进史阶段0-1（需求采集+立项入库）"
```

---

### Task 3: 撰写阶段 2（核心模块实现）与阶段 3（报价模块重构）

**文件:** 修改 `演进史.md`。

- [ ] **Step 1: 读素材**
  读 `ZK-HubX-工作清单.md` 二章「已完成模块」表（线索/日报/项目/知识库/公司主体/员工等）、`SYSTEM-OVERVIEW.md` 三章各模块小节（仅取结构概览）、根 `PLAN.md` 全篇（报价重构背景/决策/状态机）、记忆 08-14（UI 优化清单）、`报价单场景设计.md` 与 `报价单流程规范.md` 头部。

- [ ] **Step 2: 写阶段 2（核心模块实现 08-13→14）**
  五块：背景（α版原型批量落地）、做了什么（线索四池/资料/演示、日报、项目任务+成本、会议纪要、公司主体、员工管理、待办/提醒/工作台/审批中心等——按工作清单已完成项）、关键决策（前端 mock 驱动、无真实数据层）、结构数据变化（`pages/` 按业务域组织、共享 mockData/types/utils、提醒 adapter 聚合）、遗留（标注「本阶段无独立 git 提交，据代码现状与文档还原」）。

- [ ] **Step 3: 写阶段 3（报价模块重构 08-14）**
  五块：背景（0812 会议 P0 调整项、报价域需要统一工作台）、做了什么（`quotation/` 目录建立：types/quoteFlow(53单测)/QuotationContext/QuotationWorkbench/四 Stage/组件、六步配置、工作清单回写对齐、报价单独立一级菜单）、关键决策（一个工作台贯穿全流程 / 阶段导航精简 / Stage2 快捷填充）、结构数据变化（旧 QuotationList/WorkbenchPages 删除、三旧路由 loader redirect 兼容、13 态状态机）、遗留（Excel 双向导入、版本 Diff、真实 PDF 与电子签章、代理转交、超时催办、销售视图脱敏、Stage3/4 细化）。

- [ ] **Step 4: 提交**

```bash
git add "演进史.md"
git commit -m "docs: 演进史阶段2-3（核心模块+报价重构）"
```

---

### Task 4: 撰写阶段 4（报价四个工作台 + 端平台架构）

**文件:** 修改 `演进史.md`。

- [ ] **Step 1: 读素材**
  读 `packages/ui/src/app/pages/quotation/SESSION-LOG-2026-08-15.md` 全篇、记忆 08-15（四个工作台职责重划/端+平台模型/WASD/PDF/待解决技术问题）。

- [ ] **Step 2: 写阶段 4**
  五块：背景（grilling 确认职责边界：端+平台选型归工作台一、基础岗位归工作台二）、做了什么（四工作台重构 / EndpointConfig 新模型 / 原生 table rowspan 端列 / WASD 键盘漫游 / 岗位管理 / 出差驻场多地点 / 报价汇总 / PDF 下载 / 报价卡片与作废按钮）、关键决策（端+平台架构、WASD 光标位置感知调值）、结构数据变化（types 新增 EndpointConfig/PLATFORM_OPTIONS/FeatureModule.endpointId/TravelOnsiteConfig 数组化、新增依赖 html2canvas+jspdf）、遗留（光标位置判断规则冲突、原生 table 与 Arco 兼容）。

- [ ] **Step 3: 提交**

```bash
git add "演进史.md"
git commit -m "docs: 演进史阶段4（报价四工作台+端平台）"
```

---

### Task 5: 撰写阶段 5（monorepo + α/β + 报价域打通）与阶段 6（部署 + 合同域打通）

**文件:** 修改 `演进史.md`。

- [ ] **Step 1: 读素材**
  读 `HubX/docs/ALPHA-BETA-ARCHITECTURE.md` 全篇、`HubX/docs/DEPLOYMENT.md`、git 提交 `865ff80`~`6ebc5fa` 的 stat（`git show --stat`）。

- [ ] **Step 2: 写阶段 5（08-16 上午）**
  五块：背景（α/β 双版本落地、数据接缝需求）、做了什么（monorepo：packages/ui + apps/prototype+web+api、源码 src→packages/ui/src 迁移、报价域 Service 接口异步化 + quotationMutations 共享 + createHttpQuotationService + Workers 后端、全局模拟身份/待办三处/销售身份统一张三/状态机死分支清理）、关键决策（服务接缝模式作样板、α版仍 mock）、结构数据变化（services/ 目录建立、vite @ alias 复用同一份 UI）、遗留（其余域仍 mock）。

- [ ] **Step 3: 写阶段 6（08-16 下午）**
  五块：背景（部署 + 扩大 http 域）、做了什么（β后端 D1 持久化 / CORS / 种子数据 / 状态迁移校验 / DELETE 端点 / 合同域 service 接缝 + contractMutations + http 全通 / Cloudflare 三端部署 / 部署指南）、关键决策（合同域照报价样板）、结构数据变化（apps/api 路由扩展、DEPLOYMENT.md/ALPHA-BETA-ARCHITECTURE.md 建立）、遗留（线索/待办等域仍 mock、合同域服务端状态迁移校验待补、自定义域名待绑、`*.workers.dev/pages.dev` 国内被墙）。

- [ ] **Step 4: 提交**

```bash
git add "演进史.md"
git commit -m "docs: 演进史阶段5-6（monorepo+部署+合同域）"
```

---

### Task 6: 撰写「4. 当前状态快照」+「5. 附录：git 提交清单」

**文件:** 修改 `演进史.md`。

- [ ] **Step 1: 写当前状态快照**
  内容：架构现状（monorepo 结构一句话）、已通 http（报价/合同）vs 仍 mock（线索/待办等）、4 个 P0 待办现状（① 合同审批单节点部分实现 ② 报价审批页内会签不进审批中心 ③ 拆分/冲红权限矩阵未实现 ④ 财务全期次视图雏形）、遗留汇总（引用各阶段遗留项）。

- [ ] **Step 2: 写 git 提交清单**
  16 行表格：hash（短）｜日期｜类型｜内容。从 `de28286`（08-13 14:57）到 `6ebc5fa`（08-16 15:16），含当前未推送的 8 个提交。

- [ ] **Step 3: 提交**

```bash
git add "演进史.md"
git commit -m "docs: 演进史文档（当前状态快照+git提交清单）"
```

---

### Task 7: 全文交叉验证 + 终稿

**文件:** 修改 `演进史.md`。

- [ ] **Step 1: 对照 spec 验收标准逐条核验**
  ① 7 阶段齐全且每阶段五块统一；② 事实标注来源（文档/记忆/git/代码现状）；③ 不确定处标「待补充确认」且无编造；④ 阶段总览表可导航；⑤ 与 SYSTEM-OVERVIEW 等现有文档不冲突（只记变化不复制静态内容）。

- [ ] **Step 2: 一致性检查**
  日期与 git 历史一致；阶段划分与总览表一致；遗留项在「当前状态快照」中有呼应；引用路径真实存在。

- [ ] **Step 3: 终稿提交**

```bash
git add "演进史.md"
git commit -m "docs: 演进史文档全文定稿"
```

---

## Self-Review 记录

- **Spec 覆盖**：spec 五章 ↔ Task 1（说明+总览）/Task 2-5（阶段 0-6）/Task 6（快照+附录）一一对应；还原策略（有据/还原/待确认三种标注）贯穿所有撰写 Task。✅
- **占位符扫描**：无 TBD/TODO；每个 Task 的素材来源、内容五块均具体列出。✅
- **一致性**：阶段编号、日期（5-6月需求、08-13 立项、08-14 重构、08-15 工作台、08-16 分上午/下午）全篇统一；文件路径与 spec 一致。✅

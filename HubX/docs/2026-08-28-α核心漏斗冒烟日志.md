# 2026-08-28 α 核心漏斗冒烟日志

## 检查范围

本次只检查 α 版核心链路：

`线索创建 → 派发销售 → 首次跟进 → 报价提交/评估/销售报价/审批/盖章/发送/客户确认 → 主合同 → 项目指派 → 合同批准 → 开工 → 实收登记`

自动化场景位于 `packages/ui/src/app/__tests__/alphaCoreFunnel.test.ts`，直接复用各域生产 mutation、Service 与项目构建器，不另造测试流程。

## 本次结论

- 核心链路已能在 α 服务层完整走通，并覆盖重复补偿不产生重复回款。
- 修复实收双写 ID 分叉：独立 `collections` 台账与合同兼容记录沿用同一流水 ID。
- 以独立实收台账为主事实源，为合同列表、合同详情、合同看板、回款看板和回款预测提供统一只读投影。
- 主回款路由不再读取 `paymentMock.ts`；看板与预测均消费当前合同及实收台账。
- 修复付款计划日期为空或非法时甘特图整页崩溃；未定日期节点不虚构日期，也不进入时间轴。

## 验证记录

- 全量 Vitest：72 个测试文件、933 项测试通过。
- Vite α 生产构建通过（仅保留既有第三方 PURE 注释与大分包警告）。
- 浏览器检查 `/contracts/payments`：看板可见真实合同，旧 mock 合同不存在。
- 浏览器检查 `/contracts/payments?tab=forecast`：甘特图、现金流、预测明细正常，无路由错误。

## 下游实收迁移

同日继续完成项目与财务下游迁移：

- `ProjectCostAccounting` 的汇总、列表和收支抽屉改读 `CollectionContext` 投影。
- 项目 360 不再回退到合同或项目种子的 `receivedAmount`；无台账记录时按 0 处理。
- 精益交付列表、仪表盘和业务单详情统一从独立台账装配 `revenue`，原 `FD_CONTRACTS.collectionRecords` 已退出。
- 修复 `CaseMetrics` 缺少 `revenue` 导致仪表盘显示 `¥NaN`、详情显示 `¥0` 的读模型契约错误。
- 浏览器验证：项目成本页“智能家居”实收 ¥52,800；精益仪表盘累计实收 ¥406,500；CASE-001 累计实收 ¥102,500。

## 独立页面收口

- `ClosedLeads` 删除固定合同号和静态回退，按线索、合同、项目与实收共享数据生成成交读模型；华信线索现关联 `HT202606009`、项目 `3` 和实收 ¥384,000，详情入口正确进入 `/projects/3`。
- `PaymentInvoiceList` 删除 `CT202603...` 静态数据，改为按已归档主合同汇总计划、实收及 `ProjectInvoiceContext` 开票申请；冲红发票不计入有效开票金额。
- 两页统一使用 `PageShell`、二级面包屑和 `PageHeader`；财务页的筛选、详情弹窗、回款管理与开票审核入口均可用。
- 新增 2 个读模型测试文件、4 项测试；全量 Vitest 为 74 个测试文件、937 项测试通过，α 生产构建通过。

## 尚未收口

- 本轮没有改 β 持久化；线索、合同、项目、实收的 α mock Service 刷新后仍会重置，报价使用现有本地存储策略。
- 开票、交付计划、验收、售后移交及成本利润仍不是完整跨域闭环。

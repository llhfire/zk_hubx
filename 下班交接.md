# 下班交接

> 会话上下班交接文档（固定单份）。上班时先读此文件恢复上下文。
> **下班 = 写本文件 + 发布到 Cloudflare**（用户说「下班」即触发，不要再确认）。发布步骤见 `HubX/docs/DEPLOYMENT.md`「下班发布」。
> 更新时间：2026-08-18 下班

## 本次进度（本次会话做了什么）

- **锁定销售域重构模型**：线索 / 项目 / 合同是同一笔业务的三个角色视角，不是漏斗上先后三个对象。不另建「业务单」菜单。
- **五段衔接定稿**：
  1. 签约 → 自动出「未确认」项目 → 管理员确认并指定产品经理
  2. 报价成交 → 生成主合同
  3. 合同批准 → 交付启动 + 回款 Tab
  4. 需求变更可多次：N 份补充报价 + N 份补充合同
  5. 售前交接包随确认带进项目
- **阶段 0 落地**：`packages/ui/src/app/business-case/`（类型 / 状态机纯函数 / 19 个单测）
- **阶段 1 落地**：项目管理「待确认」队列 + 确认指派弹窗；线索详情状态条（LS001 待确认 / LS002 已指派）
- **术语**：`HubX/CONTEXT.md` 改为签约开启综合视图，项目状态增加「未确认」
- **下班仪式**：说「下班」即写交接 + 提交 + 发 α/β（api 有改才发 Workers）
- 浏览器已验证：`/projects` 待确认 1 → 确认指派李四；`/leads/LS001` 出现待确认条幅

## 当前状态要点

- 架构：monorepo（packages/ui + apps/prototype α + apps/web β + apps/api Workers+D1）
- 已通 HTTP：报价域、合同域；其余仍 mock
- 部署：`alpha.zkhubx.com` / `beta.zkhubx.com` / `zkhubx-api.llhfire.workers.dev`
- 销售域重构做到阶段 1；阶段 2–4 未做
- 确认项目只改列表页内存态，线索条幅仍读 `initialProjects`（阶段 2 应收成同一份状态）
- 本地：`cd HubX && npm run dev` → `localhost:5173`

## 下次待办（按优先级）

1. **阶段 2**：线索详情嵌入项目执行；项目详情新增「售前历程」Tab（只增不删）
2. **阶段 3**：报价成交生成主合同；合同批准后交付启动
3. **阶段 4**：合同标的视角 + 多份补充报价/补充合同列表
4. 项目状态提升为共享 Context，让确认后线索条幅同步
5. 旧债（可后置）：P0② 回款权限矩阵单测；测试债（3 断言失败 + react-quill jsdom）

## 踩坑提醒

- vitest 在 `HubX/apps/prototype` 下跑：`npx vitest run ../../packages/ui/src/app/business-case/__tests__/caseUtils.test.ts`
- 项目 `未确认` 对产品经理默认不可见；管理员可见并确认
- 签约触发：线索状态「合同洽谈」/「已签单」，或已有未作废合同
- 有效标的额 = 主合同已归档金额 + Σ 已归档未作废补充合同
- `featureBoard.config.json` 与 `featureBoardModel.ts` 的 `PLANNED_SEED` 需同步
- 根目录就是 git 根（`HubX/` 不再是独立嵌套仓库）
- `alphaChecklist.config.json` 已废弃

# 下班交接

> 更新时间：2026-08-31（α 假交互与销售域完成收口）

## 本次进度

### α 交互与文件闭环

- 客户/合同列表、渠道成本分析、系统配置、报价清单与正式 PDF、合同扫描件、差旅报销/借款、回款项目下钻和旧项目决算路由均改为真实行为或明确无源禁用。
- 报价功能清单支持标准五列 Excel 导入预览确认与 XLSX 导出；合同扫描件和本次会话上传文件可真实预览、下载与删除。
- 系统配置可持久化、备份和恢复；旧配置会深度合并新增销售提醒字段。

### 销售域 α

- 新增共享客户事实源：企业信用代码/个人手机号强判重、相似名提示、唯一主联系人、被引用联系人停用、开票历史、客户合并和业务链聚合。
- 线索转客户、客户列表/详情、全局搜索、报价与合同统一读取客户主档；报价和合同另存客户快照，历史业务不被主档修改覆盖。
- 新增版本化合同模板管理：草稿、不可变发布版本、复制、停用、唯一默认模板、历史版本与正文快照；历史合同版本可比较并作为新版本基线。
- 线索下次跟进承诺接入统一 Todo；联系人生日、合同到期和免费维护期到期接入统一 Reminder，可稍后提醒或人工转待办。
- 重新报价 `previousQuoteId` 与补充报价 `parentQuoteId` 分链；完成报价 Diff、补充报价合同影响、责任转交、限时代理及历史记录。
- 报价和合同新增明确无电子签名法律效力的 α 签署演示，可上传真实线下证据；合规档案只读投影现有证据并导出 HTML/JSON。
- 线索治理改为真实异常、重复、退回质检和规则概览工作台；所有处置人工确认，不再生成随机评分、自动回收或自动处罚。

### 文档、看板与架构

- `CONTEXT.md`、ADR-0098～0102、销售域实施计划、PRD、功能看板和销售域收口日志已同步。
- 根功能架构图已更新客户主档、统一待办/提醒、报价协作与签署演示的业务描述。
- β 技术图仅更新 α 本地事实源现状；β 接线、D1、Workers、生产开关和系统洞没有变化。

### 验证与发布

- `npm test -- --run`：102 个测试文件、1038 项测试全部通过。
- α、β 生产构建均通过；impeccable 检测无确定性问题，`git diff --check` 通过。
- 实页抽检客户详情、合同模板、线索治理、报价工作台和帕奇宠合同详情，应用自身控制台无运行时错误。
- 本次仅共享 UI 与文档变化，`apps/api`、schema 和 migrations 无改动；不发布 Workers、不执行 D1 迁移。
- Git 与 Cloudflare：本次下班改动统一提交并推送；α、β Pages 均以本次下班 HEAD 发布，部署列表 Source SHA 核对一致。

## 当前状态

| 项目 | 状态 |
|---|---|
| α 交互与文件闭环 | 已完成并记录真实行为/明确禁用口径 |
| 销售域 α | 客户、报价、合同、待办提醒与治理均按锁定范围完成 |
| 客户事实源 | `CustomerContext` + localStorage；β service 未接 |
| 合同模板 | α 版本化模板与正文快照完成；生产模板服务未接 |
| 签署与合规 | 仅 α 状态/证据演示，不具有电子签名法律效力 |
| 测试 | 102 文件、1038 项全绿 |
| Git / Pages | 本次下班 HEAD 已推送，α、β Source SHA 已核对一致 |
| Workers / D1 | 无改动，不发布、不迁移 |

## 下次待办

1. 销售域 α 进入观察期：优先处理实际演示反馈，不再扩张独立销售助手、AI 阶段建议或自动处罚能力。
2. 如进入 β，先为 Customer、Todo、Reminder、Template、Proxy 与 Compliance 建立服务端事实源、权限和审计，再迁移 localStorage 数据。
3. 延续原有 β 主线：真实数据阶段 3–4、B5 线上冒烟、U1 洽谈项目生成与跨域刷新幂等。
4. 继续验证帕奇宠主合同、两份补充合同、报价、回款和交付阶段付款标识在各入口的一致性。

## 踩坑与约束

- 客户是一条签约主体，不做集团母客户；企业信用代码和个人手机号是强判重键，名称相似只能预警。
- 客户主档修改只影响未来业务；报价、合同和历史开票必须读取当时快照。
- `previousQuoteId` 只表示重新报价版本，`parentQuoteId` 只表示补充报价归属，不得重新混用。
- 已发布合同模板不可原地编辑；新合同保存模板版本与渲染正文快照。
- 明确承诺才写 Todo，生日/到期机会写 Reminder；不要再造第二套销售待办或提醒中心。
- α 签署演示不得生成个人签名、不得宣称法律效力；合规档案只能投影已有证据。
- ReminderProvider 与 LeadsProvider 必须允许在旧测试或独立挂载场景下缺省客户/合同/Todo Provider，以免共享数据增强破坏模块可测试性。
- 构建仍有大 chunk 提示，但分包后所有产物低于既有 Cloudflare 上传上限；发布成功仍以 deployment list 的 Source SHA 为准。

# 下班交接

> 会话上下班交接文档（固定单份）。上班时先读此文件恢复上下文。
> **下班 = 写本文件 + 发布到 Cloudflare**（用户说「下班」即触发，不要再确认）。发布步骤见 `HubX/docs/DEPLOYMENT.md`「下班发布」。
> 更新时间：2026-08-18 下班（第二次，阶段 2）

## 本次进度（本次会话做了什么）

- **阶段 2 全部落地**（销售域三视角重构）：
  1. **ProjectContext 共享状态**：新增 `packages/ui/src/app/pages/project-management/ProjectContext.tsx`（内存态，种子 initialProjects），挂入 App.tsx Provider 链；`Projects.tsx` / `ProjectDetail.tsx` / `LeadDetail.tsx` 改为读写共享状态——管理员确认指派后线索详情条幅即时同步（原待办 #4 提前解决）。
  2. **buildPresalesTimeline 纯函数**：`business-case/caseUtils.ts` 聚合线索创建/售前跟进/报价/合同为时间倒序时间线，单测 25 个全过。
  3. **项目详情「售前历程」Tab（只增不删）**：新组件 `ProjectPresalesHistoryPanel`，上半售前交接包（需求/原型链接/售前群等）、下半只读时间线，追加在右侧 Tab 末尾。
  4. **线索详情「项目执行」主 Tab**：新组件 `leads/components/LeadProjectExecutionPanel`，项目概况+进度条+项目日报（工时汇总/日报列表）+任务管理（复用 ProjectTaskPanel）+打开项目详情；仅关联项目已确认（assigned/in_execution）时出现。
  5. **功能看板已更新**：签约后综合视图/售前历程交接包/PM 列表过滤 -> 已设计，两模块补 features 描述。
- 验证：`npm run build` 通过；business-case 单测 25/25；全量 vitest 基线对比确认 4 个失败为既有测试债（HEAD 上同样失败，非本次回归）。

## 当前状态要点

- 架构：monorepo（packages/ui + apps/prototype α + apps/web β + apps/api Workers+D1）
- 已通 HTTP：报价域、合同域；其余仍 mock（项目域本次仍是内存 Context，未接后端）
- 部署：`alpha.zkhubx.com` / `beta.zkhubx.com` / `zkhubx-api.llhfire.workers.dev`
- 销售域重构做到阶段 2；阶段 3–4 未做
- 根目录有三个未跟踪文档（ZK-HubX架构图.html / 功能清单-四域架构.md / 领域扩展建议.md），非本会话产物，未提交未部署

## 下次待办（按优先级）

1. **阶段 3**：报价成交生成主合同；合同批准后交付启动（交付启动 + 回款 Tab 联动）
2. **阶段 4**：合同标的视角 + 多份补充报价/补充合同列表（BusinessCase.extraContractIds/quoteIds 已建模未用）
3. 售前跟进 mock 目前是静态数据（ProjectPresalesHistoryPanel.PRESALES_FOLLOW_MOCK），阶段 3/4 时应与线索跟进记录收成同一份
4. 旧债（可后置）：P0② 回款权限矩阵单测；测试债（4 断言失败 + react-quill jsdom 文件级失败）

## 踩坑提醒

- **工具链会吞 `${...}` 模板插值**（会话中 Edit/Write 写入的模板字符串被替换成 `$glm-5.3_common` 占位符）：写代码时用字符串拼接 `'x' + var` 代替模板字符串，写完 grep 一次 `glm-5.3_common` 确认
- vitest 在 `HubX/apps/prototype` 下跑：`npx vitest run ../../packages/ui/src/app/business-case/__tests__/caseUtils.test.ts`
- 全量测试基线：4 个断言失败（workAttribution×2 / projectTasks×1 / dailyReportModalInit×1）+ react-quill jsdom 2 个文件级 FAIL，均为既有债
- 基线对比技巧：`git worktree add /tmp/xxx HEAD` + 软链 node_modules，可快速验证失败是否为既有
- 签约触发：线索状态「合同洽谈」/「已签单」，或已有未作废合同
- 项目 `未确认` 对产品经理默认不可见；管理员可见并确认
- `featureBoard.config.json` 与 `featureBoardModel.ts` 的 `PLANNED_SEED` 需同步
- 根目录就是 git 根（`HubX/` 不再是独立嵌套仓库）；`alphaChecklist.config.json` 已废弃
- 下班发布：α/β 前端必发，apps/api 有改才发 Workers；本次未改 api，跳过

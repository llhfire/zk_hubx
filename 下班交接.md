# 下班交接

> 会话上下班交接文档（固定单份）。上班时先读此文件恢复上下文。
> **下班 = 写本文件 + 发布到 Cloudflare**（用户说「下班」即触发，不要再确认）。发布步骤见 `HubX/docs/DEPLOYMENT.md`「下班发布」。
> 更新时间：2026-08-18（下班：报价/统一视图 grill + 文档整理 + 架构图进系统；提交并发布 α/β）

## 本次进度（本次会话做了什么）

### 1. 上班

读昨日交接、git 干净（仅未跟踪规划文档）。启动 α `http://localhost:5173/`。昨日阶段 3 人工验证路径仍未走。

### 2. 报价流程 PRD grill（已收束）

烤 `文档/PRD/PRD-报价流程管理.md`。先选「PRD 是目标规格」，随后多条按现网/可落地性改写。词表与决策：

- `HubX/CONTEXT.md` 报价相关条目
- ADR `HubX/docs/adr/0001`–`0065`
- PRD 升 v1.1

要点：已确认 ≠ 签约；合同向导不自动建；同一线索同时一份已确认；死态统一已废止；过期只是标记；撤回回待报价；可退回技术重评；会签前数字只黄灯（主报价总价>0、清单至少一行、主报价评估人天>0 是步骤完成条件）；补充报价→补充合同，废弃「补充协议」之名；主合同批准后才能开补充报价；会签/盖章读审批配置，提交时拍快照；数据/文件双来源，创建后不可改；文件流转=文件+重要信息+在线文档终稿+扫描件；客户件按中科标准件；利润率是开发人天毛利率统计项；模板只带销售侧结构；单号 `QT-YYYY-序号` 年重置；人民币含税；工期工作日只跳周末。

实现计划：`计划/当前/quote-flow-prd-implementation.md`（阶段 4.1–4.8，**未写报价收口代码**）。

### 3. 线索·项目·合同统一视图 grill（已收束）

烤 `文档/PRD/PRD-线索项目合同统一视图.md`。先选「PRD 是目标规格」，随即钉死**已确认仍不是签约**（ADR 0066）。

- ADR `0066`–`0075`
- PRD 升 v1.1
- 实现计划：`计划/当前/unified-view-implementation.md`（U1–U6，**未写代码**）

要点：未确认项目在**签约开启**时产生（洽谈/已签单/主合同草稿），合同批准只开工——**阶段 3 把 spawn 挂在 approvedAt 上，U1 要改回去**；管理员确认并指定产品经理，之后可改指；退回线索限于未确认，或未开始且无未作废主合同；三套状态机沿用现网；报价是独立对象；线索侧合同/回款入口一直在（草稿即可引用）；日报只在项目；成本合同可看、线索不看；内部项目无线索/客户/合同，不进三视角，仍做成本；主合同作废→进行中项目搁置；线索终止不停交付；线索客户与合同甲方创建后各改各的。

### 4. 文档四件套对齐规则

根 `CLAUDE.md` / `HubX/CLAUDE.md`：做计划或需求分析必须同时改 **实现计划、PRD、功能看板、架构图**。领域词看 CONTEXT，硬决策看 ADR。

看板与架构图已按上述两份计划更新（报价工作台 8 条已设计、统一视图立项/退回/搁置等已设计）。

### 5. 整理仓库文件体系

根上只留：`HubX/`、`文档/`、`计划/`、`产出/`、`CLAUDE.md`、`README.md`、`下班交接.md`、`ZK-HubX架构图.html`。各层有 `README.md` 说明用途。

- 散落需求/PRD/报价稿 → `文档/`
- 实现计划 → `计划/当前/`，旧 specs → `计划/历史/`
- 导出表/视频 → `产出/`
- HubX 根上成本截图 → `HubX/docs/design-qa/`

会议录音（`文档/会议/*.m4a`）已 `.gitignore`，不进本机提交、不进远端。录音可留本机，纪要可以提交。历史里本来就没有录音。

### 6. 架构图进系统

点左侧「ZK HubX」产品名/Logo 弹窗打开架构图。事实源仍是根目录 `ZK-HubX架构图.html`。Vite 插件（α/β）提供 `/architecture.html`。α 标签仍开功能看板。浏览器点过：弹窗、关闭、报价页、α 标签均正常。

**未写**报价阶段 4 与统一视图 U1–U6 的业务代码。阶段 3 代码仍在。

## 当前状态要点

- 架构：单一 git 仓库；monorepo 在 `HubX/`
- 已通 HTTP：报价域、合同域；项目/商机/交付计划仍是内存 mock
- 部署：`alpha.zkhubx.com` / `beta.zkhubx.com` / `zkhubx-api.llhfire.workers.dev`
- 销售域：阶段 0–3 代码在库；**阶段 3 的 spawn 点已被今天决策否定**（应改回签约开启，见 U1）；阶段 4 与统一视图只有计划
- 本地 α：`http://localhost:5173/`（多次重启过 Vite 以加载架构图插件）
- 下班已触发：提交当日改动并发布 α/β 前端（无 apps/api 改动，不发 Workers）

## 下次待办（按优先级）

1. **定开工顺序**：报价 4.1（状态机词表）还是统一视图 U1（spawn 改回签约开启）。U1 会改阶段 3 的 `ApprovalDeliveryBridge`。
2. **人工验证阶段 3 现状**（仍按当前代码：成交/已确认生成合同、批准 spawn——与今天 U1 决策不一致，验证时心里要有数）。
3. 报价阶段 4：`计划/当前/quote-flow-prd-implementation.md`
4. 统一视图 U1–U6：`计划/当前/unified-view-implementation.md`
5. 售前跟进 mock 与线索跟进收成同一份。
6. 旧债：P0② 回款权限矩阵单测；4 断言失败 + react-quill jsdom。

## 踩坑提醒

- **立项 spawn 决策与代码不一致**：今天定的是签约开启就 spawn；代码仍是 `approvedAt` 首写。上班先看 ADR 0067 和 U1，不要按 SYSTEM-OVERVIEW 旧句写新代码。
- **已确认 / 签约 / 已签单** 三个词不要混。报价终态=已确认；线索终态=已签单；签约开启=洽谈/已签单或主合同出现。
- 工具链 `${...}` 显示可能失真。判定用 `grep -c 'glm' 文件`。
- 会议录音不要 `git add`。`.gitignore` 已挡 `文档/会议/**/*.m4a` 等。
- 改架构图只改根目录 `ZK-HubX架构图.html`，系统弹窗读这份。
- 改看板同步 `featureBoard.config.json` 与 `featureBoardModel.ts` 的 SEED；跑 `npx vitest run ../../packages/ui/src/app/version/__tests__/featureBoardModel.test.ts`（在 `HubX/apps/prototype`）。
- vitest 工作目录：`HubX/apps/prototype`
- 下班发布：α/β 前端必发；`apps/api` 有改才发 Workers

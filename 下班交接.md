# 下班交接

> 会话上下班交接文档（固定单份）。下班时覆盖写最新状态；上班时先读此文件恢复上下文。
> 更新时间：2026-08-17 晚

## 本次进度（本次会话做了什么）

- **版本功能清单对比 → 功能看板（Feature Board）**：弹窗从静态对比表升级为用户与 Claude 共享的开发协作状态板（`8450d45`，11 文件，8/8 单测通过）
  - `featureBoard.config.json` 唯一事实源：14 模块，含功能条目（4 态）、α三勾选、β生产开关+开发状态（4 态）、动态备注
  - 端点 `GET/PUT /api/feature-board`：直读仓库文档，空看板被拒；无端点回退 localStorage
  - 行为约定写入 `CLAUDE.md` §功能看板，术语写入 `CONTEXT.md` §功能看板
  - Chrome 无头实测：渲染 14 行/42 checkbox/15 功能项/14 开关，开关点击写盘链路全通
- **P0 ② 回款拆分/冲红权限矩阵（WIP）**：权限矩阵纯函数 `getPeriodActionPermissions()` 已实现 + 面板两处 UI 已接线（`c37acf7`，未完成单测）
- **线上地址切换为自定义域名**：`alpha.zkhubx.com` / `beta.zkhubx.com`（`9f8ed40`）
- **版本标识与 α/β 跳转**：侧边栏版本 Tag + 弹窗线上地址+跳转按钮（`4a9d0ad`）
- **上下班规则**：已定义并写入记忆

## 当前状态要点

- 架构：monorepo（packages/ui + apps/prototype α版 + apps/web β前端 + apps/api β后端 Workers+D1）
- 已通 HTTP：报价域、合同域；其余域仍 mock
- 部署：β后端 Cloudflare Workers（`zkhubx-api.llhfire.workers.dev`）；前端绑定 `alpha.zkhubx.com` / `beta.zkhubx.com`（Cloudflare Pages）
- 本地 dev：α版 `npm run dev -w apps/prototype` → `localhost:5173`

## 下次待办（按优先级）

1. **P0 ② 回款拆分/冲红权限矩阵收尾**：矩阵函数与 UI 接线已完成，待补单测 + 构建验证 + 演进史 P0 ② 标完成
2. **P0 ③ 财务全期次回款 + 开票视图**：一屏查看项目全期次回款与开票状态
3. **清测试债**：`projectTasks.test` / `workAttribution.test` / `dailyReportModalInit.test` 3 个断言失败 + `components.test` / `ProjectDetail.test` react-quill jsdom 崩溃——monorepo 迁移遗留
4. 可选：合同域服务端状态迁移校验（照报价域样板）

## 踩坑提醒

- vitest 需在 `HubX/apps/prototype` 目录下跑（`cd apps/prototype && npx vitest run <文件>`），根目录直跑不解析 `@` alias
- 合同域 `'standard'` 四步模式仍保留，勿误删
- `alphaChecklist.config.json` 已废弃（迁移进 `featureBoard.config.json`），勿再引用
- `versionMatrix.ts` 的 `VERSION_MODULE_SEEDS` 仅供种子初始化，运行时状态以 `featureBoard.config.json` 为准
- 看板端点 PUT 校验 schema（`isValidFeatureBoard`），空 `modules` 数组非法（防误清空）

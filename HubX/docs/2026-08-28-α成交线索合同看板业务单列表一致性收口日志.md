# α 成交线索、合同看板与业务单列表一致性收口日志

## 本轮范围

仅调整 α 原型页面，不修改 β Web、API 或数据协议。收口 `/leads/closed`、`/contracts/kanban`、`/financial-delivery/cases` 三个高频入口，并保留此前已接入的线索、合同、项目和实收上下文。

## 完成内容

- 三页统一接入 `PageShell`、`PageHeader`、`ProcessMetricGrid`；一级页面不展示面包屑，标题统一为 20px。
- 已成交线索改为四项摘要与共享筛选栏，补充筛选结果、重置入口和查看操作的 Tooltip/无障碍名称。
- 合同看板移除重阴影、自绘 24px 指标和 24px 模块标题；四项摘要改用共享组件，模块标题统一为 16px，双列内容在窄屏自动转单列。
- 业务单管理移除 28px 彩色指标和重复页面内边距，补充筛选结果与重置入口；表格操作只保留已注册的详情穿透。
- 业务单导出由无行为按钮改为真实 CSV 下载；未注册的编辑入口被移除，新建操作明确提示业务单由签约链路生成。

## 验证结果

- 全量 Vitest：86 个测试文件、980 项测试全部通过。
- α 构建：`npm run build` 通过，仅保留仓库既有的 Gantt 注释与大 chunk 提示。
- 浏览器验收：成交线索与业务单筛选/重置、合同时间筛选、业务单生成规则均正常；CSV 实际导出 5 条业务单。
- 响应式验收：1200px 与 760px 下三页无页面级横向溢出；合同看板由双列正确切换为单列，控制台无错误。

## 后续工作

继续收口差旅二级管理页：宿舍管理、费用标准和 AI 财务审核看板；重点处理大标题、高密度内联样式和图标操作规范。

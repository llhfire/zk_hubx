# PLAN.md — 报价模块重构

> 锁定日期：2026-08-14
> 状态：已锁定（grill-with-docs 达成共识）
> 需求来源：`报价单流程规范.md` + `报价工作台/报价工作台1~4`

## 背景

重构报价模块，把现有「线索/项目详情里的六步报价配置器」升级为**独立报价管理模块**，覆盖「功能清单 → 人天评估 → 销售报价 → 三人并行审批 → 盖章 → PDF 交付」全链路。

## 决策清单（已锁定）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 实现范围 | 四工作台全做 |
| 2 | 现有代码 | 新建独立报价模块，现有六步配置器后续淘汰 |
| 3 | 审批流 | 复用通用审批中心（需扩展真正的「会签」多人并行） |
| 4 | Excel | 表单 + Excel 导入导出都做（复用 exceljs） |
| 5 | 报价单关联 | 关联线索（线索→报价→合同漏斗） |
| 6 | 角色模拟 | 报价模块内角色切换器（6 角色） |
| 7 | 状态机 | 完整状态机（见下） |
| 8 | PDF | 简化 PDF（打印预览 + 预设印章图片） |
| 9 | 报价权限 | 产品经理和销售都可进行增项报价、提交审批 |

## 角色（6 个，mock）

| 角色 | 身份 | 职责 |
|---|---|---|
| 产品经理 | 张产品 | 生成/修改功能清单、核对人天、转派销售、增项报价、提交审批、作废草稿 |
| 罗总 | 技术负责人 | 评估人天、技术疑义退回、审批 |
| 黄奕 | 销售部负责人 | 审批 |
| 闵总 | 企业决策层 | 审批 |
| 黄海 | 董助 | 盖章 |
| 销售 | 李销售 | 增项报价、提交审批、下载 PDF |

## 状态机（完整）

```
草稿 → 功能清单已确认 → 人天评估完成 → 已转派销售 → 报价已汇总
  → 审批中(三人并行) → 待盖章 → 已盖章 → 已发出
      ├→ 成交 → 进入合同
      ├→ 重新报价 → 新建版本
      └→ 待跟进
  审批中 → 驳回 → 退回销售 → 修改重提 → 审批中（全员重审）
  审批中 → 销售撤回 → 草稿
  任意 → 作废 → 已作废（不可恢复）
  人天评估 → 技术疑义退回 → 功能清单已确认（PM 补充后重提）
```

## 数据模型

```
Quote（报价单，关联 leadId → contractId）
├─ id / quoteNo（ZK-YYYYMMDD-NNN）/ version（v1.0）
├─ status（状态机）
├─ basicInfo：项目名称/类型/创建人(PM)/技术评估人(罗总)/需求描述/客户/有效期
├─ featureList（功能清单）
│   └─ 模块 → 子功能 → 描述/备注/技术疑义状态
├─ evalSheet（罗总人天评估）
│   ├─ activeRoles[]（动态岗位列）
│   ├─ evaluationUnits[]（切片粒度：MODULE_PACK / SUB_GROUP / SINGLE）
│   │   └─ manualWorkload（各岗位纯手动人天）/ risk / issue / remark
│   └─ manualWorkDays（工期）/ techSolutionNote
├─ salesAddition（销售增项）
│   └─ 岗位名称/人数/天数/日均单价/小计/事由
├─ travelOnsite（出差驻场）/ otherCost（其他成本）
├─ summary（报价汇总）：总人天/总报价/付款方式/占比
├─ approval（三人并行会签节点 + 盖章节点）
├─ versionHistory（版本快照，不可覆盖）
└─ pdf（盖章 PDF 水印）
```

## 四个工作台

| 工作台 | 操作人 | 核心 |
|---|---|---|
| 1 功能清单 | 产品经理 | Excel 导入/在线编辑/模块树/技术疑义看板/版本快照/转派罗总 |
| 2 人天评估 | 罗总 | 动态岗位列/纯手动工时/切片（整包/合并/单项）/工期/疑义退回/提交 PM |
| 3 销售报价 | 销售 | 7 步向导（基本信息→前端→后端→增项→出差→其他成本→汇总校验）/罗总人天只读/提交审批 |
| 4 审批盖章 | 黄奕/罗总/闵总/黄海 | 三人并行会签/穿透式审查/驳回必填/全员重审/盖章/PDF 下载 |

## 7 步报价向导（工作台3）

1 基本信息 → 2 前端配置 → 3 后端配置 → 4 其他岗位(罗总人天只读+销售增项) → 5 出差驻场 → 6 其他成本 → 7 报价汇总与校验

系统校验：∑分项人天=总人天；∑成本小计=总报价；付款比例=100%；开启的差旅/第三方成本金额>0。

## 审批流（复用通用审批中心 + 扩展）

- 三人并行会签（黄奕/罗总/闵总），全部通过 → 待盖章
- 任一驳回（意见必填）→ 退回销售 → 全员重审
- 黄海盖章 → 已盖章 → 生成 PDF
- **需扩展 approvals 模块**：现有「会签」仅类型定义，`decideApproval` 未实现多人并行，需补全。

## 实现要点

1. 新建 `src/app/pages/quotation/` 模块（类型/Context/mockData/工作台页面/组件）
2. 报价模块内角色切换器（Context 维护当前角色）
3. 复用 `exceljs` 做 Excel 导入导出
4. 复用 `exceljs` 依赖；PDF 用浏览器打印 + 预设印章图片
5. 扩展 `approvals` 支持会签（多人并行、全通放行、任一驳回）
6. 路由挂载到 `报价管理` 菜单

## 范围外（本次不做）

- 真实登录/权限系统（用 mock 角色切换）
- 真实电子签章/哈希校验（简化 PDF）
- 扣子（Coze）API 对接（功能清单用 Excel 导入 + 手动编辑模拟）

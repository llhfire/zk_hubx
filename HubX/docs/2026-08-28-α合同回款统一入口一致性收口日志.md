# α 合同回款统一入口一致性收口日志

## 本轮范围

仅调整 α 原型中的合同回款页面，不修改 β Web、API 或持久化协议。覆盖主入口 `/contracts/payments`、旧入口 `/contracts/payments-v2`、回款行动队列及回款预测页签。

## 完成内容

- 将 `/contracts/payments` 设为唯一回款工作台；旧 `/contracts/payments-v2` 自动重定向到主入口，消除两套页面框架并行。
- 页面统一接入 `PageShell`、面包屑和 20px 页头；看板与预测保留在同一页签框架中。
- 回款看板复用 `ProcessMetricGrid`、`FilterBar` 与 70:30 `ProcessWorkspace`，统一四项业务摘要、结果反馈、重置逻辑和主辅区布局。
- 看板继续读取合同上下文与实收台账投影，不恢复独立 `paymentMock` 数据源；行动清单改为导出当前筛选结果的真实 CSV。
- 预测甘特图、现金流预测和单合同明细统一工具栏、主题语义色、表格横向滚动与窄屏断点。
- 图标操作补齐 Tooltip 和含合同号、期次的无障碍名称。

## 验证结果

- 全量 Vitest：87 个测试文件、981 项测试全部通过；定向回归覆盖共享指标及 `ProcessWorkspaceMain`/`ProcessWorkspaceAside` 渲染契约。
- α 构建通过，仅保留仓库既有的 Gantt 注释与大 chunk 提示。
- 浏览器验收覆盖主入口、预测页签、旧路由重定向、筛选/重置和真实 CSV 下载。
- 1200px 与 760px 下均无页面级横向溢出；页头 20px、模块标题 16px，控制台未发现错误事件。

## 后续工作

继续检查 α 人资域：员工列表、考勤、绩效、级别费率及人资费用页，重点核对页头层级、共享摘要、筛选反馈、模块标题和窄屏内容起点。

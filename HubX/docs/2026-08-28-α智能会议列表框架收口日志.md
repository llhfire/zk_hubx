# 2026-08-28 α 智能会议列表框架收口日志

## 本次范围

仅调整 α 智能会议列表页面、列表摘要与搜索纯函数，未修改 β Service、Workers 或 D1 链路。

## 已完成

- `SmartMeetingListPage` 移除 960px 固定居中宽度，复用 `PageShell`、`PageHeader`、`FilterBar` 和统一指标网格。
- 页头统一为 20px 标题、简短范围说明和单一主操作；一级列表不增加面包屑。
- 本月概览统一展示已沉淀、待确认、已确认和已归档四项指标。
- 搜索与状态筛选进入同一筛选栏，补齐结果数量、筛选重置、无结果空态和首次新建入口。
- 纪要卡片改为全宽可聚焦条目，支持 Enter 和空格键进入详情；窄屏下筛选项和卡片信息自动纵向排列。
- 参会人由内部用户 ID 投影为姓名；关键词搜索真实覆盖主题、决议、正文、行动项、可见业务引用、参会人姓名及 ID。

## α 边界

纪要列表继续读取 α 浏览器本地数据。服务端查询、分页和刷新持久化属于 β 接线，本次未扩展。

## 验证

- 智能会议定向 Vitest 通过，新增参会人姓名投影和决议/行动项关键词测试。
- α 构建通过。
- 1440×1000 浏览器验收：内容宽度 1192px、4 项指标、3 条种子纪要，标题 20px，无水平溢出。
- 搜索、状态筛选、重置、空态和键盘进入详情均正常；760px 窄屏无水平溢出，筛选与列表按单列排列，控制台无报错。

## 后续

继续 α 全页面一致性计划，下一批优先核对日报、财务和系统配置一级列表，再批量补齐二级以上页面面包屑。

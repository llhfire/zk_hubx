# ZK HubX 演进史

> 项目演进档案（自用备忘）｜跨会话存档
> 覆盖范围：2026-05 需求采集 至 2026-08-16 当前

## 一、文档说明

### 用途

项目演进档案（自用备忘），跨会话存档——下次接续开发前翻一眼就能知道过去发生了什么。记录 ZK HubX 从需求采集到当前（2026-08-16）的完整演进历史，以时间线分阶段、里程碑粒度呈现，不逐提交展开。

### 覆盖范围

2026-05 需求采集 至 2026-08-16 当前。

### 如何使用

- 先看「二、阶段总览表」定位任意历史节点；
- 再读对应阶段详述（「三、分阶段详述」）；
- 看现状直接读「当前状态快照」（后续章节）。

### 信息源与可信度

信息主要来自：git 提交历史、跨会话开发记忆、现有文档（工作清单 / PLAN / SYSTEM-OVERVIEW / ALPHA-BETA-ARCHITECTURE / 会话日志）与代码现状。因部分阶段在 git 中无独立提交，特约定以下三种标注，读者可据此判断每条信息的可靠程度：

- **有据**：直接来自 git 提交 / 跨会话记忆 / 现有文档，可追溯；
- **据代码现状还原**：git 无独立提交的阶段，凭现有文档与代码逆向还原；
- **待补充确认**：还原不了或不确定处，明确标注，绝不编造。

## 二、阶段总览表

| 阶段 | 日期 | 主题 | 一句话 |
|---|---|---|---|
| 0 | 2026-05~06 | 需求采集 | OA需求文档、原始需求池、录音分析、交付SOP |
| 1 | 2026-08-13 | 立项与代码入库 | first commit、工作清单对齐、单一仓库统一 |
| 2 | 2026-08-13→14 | 核心模块实现 | 线索/合同/回款/项目/日报等90%模块 |
| 3 | 2026-08-14 | 报价模块重构 | 六步报价、UI优化、工作清单回写 |
| 4 | 2026-08-15 | 报价四个工作台 | 端+平台架构、WASD键盘漫游、PDF下载 |
| 5 | 2026-08-16 上午 | monorepo+前后端 | α/β双版本、服务接缝、报价域http |
| 6 | 2026-08-16 下午 | 部署+合同域 | D1、Cloudflare部署、合同域http |

## 三、分阶段详述

### 阶段 0：需求采集（2026-05~06）

#### 背景与触发

中科集团是「软件外包 + 自运营投放获客」复合型公司，业务管理层希望把「线索→报价→合同→回款→项目→交付」全链路的管理流程和规则固化进系统，减少人工传递和口头说教。此前业务数据分散在 Excel 表格、项目管理简表与日常沟通中，上下文割裂、项目成本算不清，需要一套内部运营管理系统承接。2026-05 起通过原始需求池、会议录音、OA 模块规划等多个渠道集中采集需求。

#### 做了什么

- **`原始需求池.md`**：三批需求沉淀。2026-5-12（公司主体资料下载、合同统计与工资维护）；2026-5-15（线索分配后管理、销售日报模板）；2026-5-21（站内消息提醒）。三批覆盖公司资料、成本统计、日报、消息提醒等核心诉求。
- **`OA需求文档.md`**：OA-1.0 项目模块规划，明确阶段目标（阶段 1 线索/跟进模块导入引导使用；阶段 2 项目管理 + 成本核算，牵扯人工成本设置、项目报销提交、日报），并给出项目模块属性表、列表/详情/关联线索/关联日报/跟进记录/项目文档等页面结构设计。
- **`录音分析结果.md`**：会议核心结论四个重点——①权限体系需支持「权限组→岗位→人员→菜单/功能/按钮」逐级控制；②项目详情页必须保留原有信息、只能原基础上增功能；③成本核算核心是「按人员工时计算项目实际成本」，而非简单展示工资；④线索和项目准备统一为可持续跟踪过程的「业务单」。同时确立底层数据原则：人员成本以员工综合成本为基础、项目成本以日报工时为入口、公共成本按规则分摊、所有结果最终沉淀到项目成本明细。
- **`软件外包项目交付执行 SOP 1.md`**：软件外包项目全流程交付标准作业程序，覆盖合同交接、项目启动准备、项目交付执行、资质备案 & 上架、测试验收、运维支持、项目总结七大板块，并纳入域名、ICP 备案、小程序备案、APP 上架、软著等上线资质与 SSL 证书、大模型 Token、第三方接口、云资源等配套资源规范。
- **PRD 文档族**：需求逐步沉淀为模块 PRD。已成型文档包括线索管理、日报、报价 Tab、审批流程、知识库、待办提醒等 PRD，以及 `文档/superpowers/specs/` 下的设计规格（项目、公司主体、成本核算、员工管理等），归档索引见 `进度对齐列表.md` 第五节「文档归档索引」。

#### 关键决策

- **定位为「中科集团内部管理数字化」**：明确「不做 AI、不做对外售卖、不做泛化」，只做中科集团自己的管理数字化（见 `ZK-HubX-工作清单.md` 第一章）。
- **需求先固化流程规则**：把业务管理层的流程和规则固化成系统，替代人工传递；线索从获客到签约再到交付回款上下文连续不割裂。
- **成本核算是系统底层核心**：按人员工时计算项目实际成本；日报是核心数据来源，手工补录历史工时也必须写入日报结构，禁止在成本页单独造一套工时数据。

#### 结构·数据变化

- 需求形态从零散信息固化为分层文档族：三批原始需求（5-12 / 5-15 / 5-21）→ OA 模块规划 → 录音结论 → 模块 PRD + superpowers 设计规格 + 交付 SOP。
- 业务对象从 Excel/项目管理简表向系统结构化字段演进，如项目模块属性表、日报模板、成本核算公式、权限逐级模型等均在文档中定型。

#### 遗留与风险

- 部分 PRD 标记 🔧 需修订/待定稿：报价 Tab、审批流程管理、知识库、开发对齐-报价 Tab 等。
- 「业务单」（线索/项目统一容器）尚未完全明确——是替代线索/项目还是作为统一容器未定，录音在「财务」处中断，需补充确认。
- 成本核算待确认问题较多：员工工作天数、每日工作小时数、公共成本分摊范围、成本计算时点、历史数据规则等。

（依据：原始需求池.md / OA需求文档.md / 录音分析结果.md / 软件外包项目交付执行 SOP 1.md / ZK-HubX-工作清单.md / 进度对齐列表.md）

### 阶段 1：立项与代码入库（2026-08-13）

#### 背景与触发

需求采集与 PRD 沉淀告一段落后正式立项。2026-08-13 一天内完成了立项所需的核心文档建立：工作清单（产品定位与模块全景）、进度对齐列表（α/β/v1.0 版本定义与 38 模块状态）、实施计划（三版本推进路径）。同时此前的 HubX 前端代码位于独立位置，需要统一收拢进根仓库做单一仓库管理。

#### 做了什么

- **`ZK-HubX-工作清单.md`**（更新时间 2026-08-13）：第一章确立产品定位与建设目标（含「不做的事」），第二章给出模块全景与开发状态（已完成/开发中/待安排），第三章起按业务域拆解工作清单。
- **`进度对齐列表.md`**（创建时间 2026-08-13）：给出 α/β/v1.0 版本定义与验收标准、38 个模块的 α 版文档清单与完成度统计（✅ 19 / 🔧 5 / ⏳ 14）、β 版开发清单与预估工时、里程碑时间线（α 版启动 2026-08-13）、文档归档索引。
- **`实施计划.md`**：确立「α版（想清楚）→ β版（跑通）→ v1.0（做稳）」三版本递进推进路径，并说明为什么分三步。
- **git 首次提交系列**（2026-08-13）：de28286 / c8d0af3 / 73fbd1e / 8969b90 四个 first commit（README 占位），随后 97d50e0 建立 .agents / .claude 等配置，最后 c03ecb7「feat: 将 HubX 前端代码并入根仓库，统一为 ZK HubX 单一仓库」完成代码入库。

#### 关键决策

- **α 版原型先行**：不写代码、只输出文档，文档不齐不开工——先校验认知对齐，避免外包项目最大的「理解偏差」风险。
- **单一仓库统一管理**：HubX 前端代码并入根仓库，统一为 ZK HubX 单一仓库，根目录承载项目文档族、HubX/ 承载应用代码。
- **三版本递进**：α 版校验认知对齐 → β 版校验设计可行性 → v1.0 校验生产可用性，每层是下一层的前置条件。

#### 结构·数据变化

- 根目录项目文档族成型：需求（原始需求池 / OA 需求文档 / 录音分析结果）→ 清单（ZK-HubX-工作清单）→ 计划（实施计划）→ 进度对齐（进度对齐列表）。
- 代码结构：HubX 前端从独立位置并入根仓库，成为单一仓库的组成部分（c03ecb7），根仓库的 git 历史自此与 HubX 代码合并管理。

#### 遗留与风险

- 文档清单不全待补：进度对齐列表 α 版 38 个模块中 14 个待编写、5 个需修订，需逐步补齐。
- 各域 PRD 未全定稿：销售域（合同/回款/线索自动分配）、交付域（线索→项目同源、销售日报模板等）、管理域（工资管理、运营成本分摊等）、协同域（企微集成、知识库深度）均有缺口。
- α 版封版时间待定：里程碑时间线中 α 版封版及后续 β/v1.0 节点目标日期均为待定。

（依据：git 提交记录 / 实施计划.md / 进度对齐列表.md / ZK-HubX-工作清单.md）

### 阶段 2：核心模块实现（2026-08-13→14）

#### 背景与触发

阶段 1 完成单一仓库统一与工作清单对齐后，进入核心模块集中实现期。工作清单（08-13 版）中绝大部分模块当时仍标「待安排」或「开发中」，08-13→14 两天内把线索、合同、回款、项目、日报等核心模块批量落地。〔据代码现状还原〕：本阶段未独立提交，代码混在后续兜底提交 `58c53a0 "1"`（2026-08-15）与 `1c27682 "分支之前的版本"`（2026-08-16）中，模块完成度以代码现状与 SYSTEM-OVERVIEW 遍历结果为准。

#### 做了什么

- **线索管理**：公海/我的/已成交/垃圾四池流转，认领/分配/回收/垃圾标记全链路；详情页资料管理（统一文件库、来源标签、去重合并）、演示记录与网址展示、跟进记录；客户状态 8 态、意向等级与来源枚举统一定义。
- **合同管理**（后续成长为全仓最大模块）：新建合同自动带入线索基础信息；编辑自动生成新版本、历史版本只读；审批流（发起/商务/财务/法务/总经理 5 步）；审批通过后归档（archiveFinalContract）；合同列表集中入口；软件销售 / 技术服务 / 云服务 3 套合同模板（含各自关键条款）。
- **回款与发票**：期次自动拆分、金额联动；财务登记实际回款；开票登记与附件上传；回款状态由明细累计金额自动计算（getPaymentPeriodMetrics）。
- **项目管理**：详情页基础信息 / 进度跟踪 / 人员分配；任务管理（ProjectTaskPanel，状态机流转、完成强制进度 100）；成本核算（ProjectCostPanel，人工 / 差旅 / 商务 / 第三方 + 利润率）。
- **日报**：模板化填报（岗位 → 规则 → 模板 → 内容三级建模）、工时关联项目、最近记录快捷填充、工时×时薪人工成本（buildRDCostDetails）；8 种字段类型、4 个模板组件、提交逐项校验。
- **会议纪要**（MeetingManagement）：关联线索 / 项目，结论记录。
- **消息提醒**（reminders + ReminderBell 铃铛）：审批 / 日报 / 线索 / 合同 4 类 adapter 聚合，每分钟刷新，9 种提醒类型。
- **待办中心**（TodoCenter）：待办聚合与提醒，订阅审批结果自动完成 / 取消待办。
- **工作台**：首页 Dashboard（摘要卡 / 快捷入口 / 业务概览 / 待跟进线索）+ 个人工作台 PersonalWorkbench（六项指标 + 概览 / 任务 / 能力 / 项目四 Tab）。
- **审批中心**（ApprovalCenter）：通用审批双层模型（模板定义 + 业务绑定），支持单人 / 或签 / 会签策略。
- **企微集成基础**（WeComIntegration）：通讯录同步、集成配置、数据边界（姓名 / 手机 / 部门等由企微维护，职级 / 成本 / 绩效由 HubX 维护）。
- **公司主体与员工**：6 个签约主体（简称 / 合同号前缀 / 合同模板）；员工列表与岗位配置，五维能力建模、职级时薪级联更新（updateLevelRate 同步级联所有员工 standardHourlyRate，喂给成本核算）。
- **线索成本、客户管理、数据报表、资产、售后运维、供应商、知识库、会议、全链路 ROI、AI 助手、系统管理**等扩展模块同步落地（〔据代码现状还原〕：以 SYSTEM-OVERVIEW 各节遍历结果为准）。

#### 关键决策

- **前端 mock 驱动、无真实数据层**：全部数据为页面内 useState + 模块级 mockData + 少量 context，无 axios / fetch / React Query / Redux / Zustand（〔据代码现状还原〕，SYSTEM-OVERVIEW 明示「无真实后端」）。
- **「页面局部状态」vs「业务目录共享 mockData / types / utils」判断原则**：简单模块用「列表页 / 详情页」并列，复杂模块把页面 / 类型 / mock / 模板 / 弹窗收进同一子目录；改页面前先确认数据归属，不默认去找 API 层（〔据代码现状还原〕，见 HubX/CLAUDE.md）。
- **复杂逻辑抽纯函数 + Vitest**：状态机、口径计算独立成 `.ts` 纯函数并配单测，标杆为交付计划 utils（全项目测试最完整）（〔据代码现状还原〕）。
- **跨模块靠 import 共享 mock 模拟真实业务依赖**：contractCostData import project-management mockData、reminders import daily-report types，而非全局 store（〔据代码现状还原〕）。
- **提醒系统 adapter 聚合**：审批 / 日报 / 线索 / 合同四类提醒经 adapters 聚合后统一排序，改提醒逻辑优先复用纯函数而非散落页面（〔据代码现状还原〕）。
- **工作清单回写对齐**：工作清单曾严重低估完成度，08-13 对照代码逐项回写对齐，把「已完成」标记到真实水平（依据：开发记忆）。

#### 结构·数据变化

- 页面按业务域组织 `src/app/pages/`：daily-report / lead-cost / contract-cost 等「模块目录」形态，整模块文件集中在同一子目录（〔据代码现状还原〕）。
- 全局状态收敛为 Context Provider 嵌套（阶段 2 时约 9 个：Integration / Approval / Todo / Feedback / Reminder / Employee / JobWorkConfig / Contracts / ProjectInvoice；阶段 3 起并入 QuotationProvider 达 10 个），路由集中 routes.tsx（〔据代码现状还原〕）。
- MainLayout 统一侧边栏 / 顶栏 / 日报入口 / 提醒铃铛 / 意见反馈弹窗；报价入口此时尚未独立为一级菜单（阶段 3 才调整）（〔据代码现状还原〕）。
- 数据层三态：绝大多数模块 Context 内存 mock + 纯函数计算；审批配置 / 待办 / 开票 / 反馈等少数模块用 localStorage 持久化；全程无网络层（〔据代码现状还原〕）。
- 线索详情成为全仓最大单文件之一（8 面板双栏）；合同模块成长为最大模块（16 个子组件 + 版本快照 + 3 模板 + 补充协议）。

#### 遗留与风险

- **P0 待办三项**（依据：开发记忆）：① 审批流收敛为总经理单节点（当前合同 / 报价仍为多节点，审批单节点改造为卡点）；② 回款拆分 / 冲红状态权限矩阵（四种回款×开票组合）；③ 财务全期次回款 + 开票视图。
- 各模块深度待办（工作清单 P1 / P2）：付款比例模板保存与区分、回款逾期判断、列表操作区紧凑度、人工成本按工作性质分组、跨月 / 跨年日报时间轴等。
- 待确认事项 5 项：工时评估模板拆分口径（按岗位 vs 按工作性质）、回款逾期信息展示位置、成本默认排序规则、报价模板视觉稿、财务登记回款字段规则。
- 回款 / 开票 / 拆分 / 冲红相互关联，应形成统一状态机，避免各页面分别判断导致操作权限不一致（工作清单风险 1）。
- 线索→项目同源、线索自动分配、项目下合同 / 回款统一视图等仍待安排。

（依据：SYSTEM-OVERVIEW.md / ZK-HubX-工作清单.md（08-13 回写版）/ 开发记忆 zk-hubx-work-checklist-progress / 兜底提交 58c53a0「1」与 1c27682「分支之前的版本」；本阶段无独立提交，多数表述为〔据代码现状还原〕）

### 阶段 3：报价模块重构（2026-08-14）

#### 背景与触发

既有报价能力是「线索 / 项目详情页内嵌的六步报价配置器」，只能覆盖单个报价的配置与汇总，无法承接「功能清单 → 人天评估 → 销售报价 → 三人并行审批 → 盖章 → PDF 交付」全链路；同时 08-12 会议对报价提出一批 P0 调整项（金额区信息顺序、功能清单→详细报价单数据生成、草稿 / 编辑 / 提交审批、移除提交审批页重复技术评估上传、审批提交前灰态展示）。08-14 报价文档族集中成型——`报价单流程规范.md`（覆盖需求到出单全链路、支持文件流转 + 表单数据流转双模式）、`报价单场景设计.md`（确立「一个工作台贯穿报价全流程」核心理念）、`报价流程严格审查报告.md`（审查发现角色交接、数据权限、版本追溯、异常状态四类漏洞）、`报价单流程图.html`，随后 `PLAN.md` 以 grill-with-docs 方式锁定决策清单。〔据代码现状还原〕：本阶段未独立提交，代码混在兜底提交 `58c53a0` 与 `1c27682` 中，以 PLAN / 开发记忆 / 代码现状为准。

#### 做了什么

- **报价文档与计划**：报价单流程规范、场景设计、严格审查报告、流程图（.html）、PLAN.md 决策清单锁定（9 项决策 + 6 角色 + 完整状态机 + 数据模型 + 四工作台设计 + 7 步向导设计）。
- **报价模块从零到工作台贯穿全流程**：新建 `pages/quotation/` 目录（types / quoteFlow.ts（56 条单测〔据代码现状〕）/ QuotationContext（唯一状态源）/ QuotationWorkbench（工作台外壳）/ QuotationCenter（列表）/ defaultFeatures（默认功能模板）/ stages/Stage1~4 / components/RoleSwitcher、QuoteTimeline）。
- **四工作台职责落地为「一个工作台 + 4 个 Stage」**：Stage1 功能清单（PM 张产品）→ Stage2 人天评估（罗总）→ Stage3 报价配置（销售：增项、出差驻场、其他成本、汇总、付款方式）→ Stage4 审批盖章（会签三人 + 董助）；`deriveStage(status)` 按状态决定渲染对应 Stage（〔据代码现状还原〕：实现形态为同一外壳内的 4 个 Stage，与 PLAN 四工作台设计收敛一致）。
- **六步配置流程重组**：原「前端→后端→岗位→出差→其他→汇总」六步并入新的 Stage 分工，销售端报价配置仍保留分步；PLAN 中设计的 7 步向导（Stage3QuoteWizard）最终未被引用，属重构前遗留（〔据代码现状还原〕，见 SYSTEM-OVERVIEW 3.4）。
- **13 态状态机**（QuoteStatus）：draft / feature_confirmed / eval_completed / assigned_sales / quote_summarized（预留）/ auditing / rejected / pending_stamp / stamped / sent / deal / pending_followup（预留）/ voided；14 个动作（QuoteAction 枚举，含 create 与 new_version）。
- **6 mock 角色 + 角色切换器**：pm（张产品）/ tech（罗总）/ sales（李销售）/ sales_manager（黄奕）/ decision（闵总）/ assistant（黄海）；RoleSwitcher 供原型切换视角，阶段权限按「editable / readonly / locked」收敛。
- **UI 优化**（依据：开发记忆）：`/quotation` 提为独立一级菜单（IconQuote），从财务管理移出；线索详情报价 Tab 新增「新建报价」按钮 + Drawer 打开工作台；工作台外壳角色切换器置顶、阶段导航精简为紧凑条状带序号、项目信息条单行；Stage1 功能清单默认填充约 18 项（7 模块）、模块列 hover 显示 + 号新增子功能、按钮改「导入」「校验并提交工时评估」；Stage2 人天评估整表横向滚动、表头不换行、新增「按模块」「按子功能」快捷批量按钮、方向键 + Tab 连续填充、提交校验未填格子红框高亮、输入框默认空白不显示 0.0；Stage2 角色切换 RoleSwitcher 改为 Button 组。
- **清理与兼容**：旧 `QuotationList.tsx` 已删除（可考于 58c53a0，516 行移除）；`QuotationWorkbenchPages.tsx` 与 `workbenches/` 的删除仅存于开发记忆，git 历史无对应文件记录（〔据开发记忆〕）；旧三路由（/quotation/eval|quote|approval/:quoteId）用 loader redirect 收敛到统一工作台 `/quotation/:quoteId`（〔据代码现状还原〕，见 SYSTEM-OVERVIEW 2.3）。
- **工作清单回写对齐**：报价六步配置标记 90%，0812 会议 P0 待办项同步写入工作清单，报价模块遗留项逐条登记（依据：开发记忆）。

#### 关键决策

- **一个工作台贯穿全流程**：功能清单、人天评估、报价、审批盖章在同一工作台内完成，不同角色在不同步骤打开同一工作台，看到的是当前步骤内容（依据：报价单场景设计）。
- **工作清单 0812 会议 P0 调整纳入实现**：报价金额区信息顺序（原始金额→上浮信息→最终金额）、功能清单→详细报价单数据生成、草稿 / 编辑 / 提交审批、移除提交审批页重复技术评估上传、审批提交前灰态展示（依据：ZK-HubX-工作清单.md 第四章）。
- **新建独立报价模块，旧六步配置器后续淘汰**（PLAN 决策 2）：不是就地改造，而是独立 quotation/ 模块重写。
- **审批流复用通用审批中心并扩展「会签」多人并行**（PLAN 决策 3）：三人并行会签（黄奕 / 罗总 / 闵总）、任一驳回即清空全员重审，需扩展 approvals 的 decideApproval 多人并行能力。
- **角色切换改 Button 组**：原 checkable Tag 存在 onClick 问题，UI 优化时改为 Button 组（依据：开发记忆）。
- **产品经理与销售均可增项报价、提交审批**（PLAN 决策 9）；报价权限按 Stage 收敛（Stage4 内按状态限会签人 / 盖章人 / 销售）。

#### 结构·数据变化

- `pages/quotation/` 目录建立，模块目录形态（类型 / Context / mock / 纯函数 / 页面 / 组件集中），`quoteFlow.ts` 为纯函数层配 56 条单测（〔据代码现状〕：vitest 实测通过 56）。
- 13 态状态机 + 版本快照：`createNewVersion` 深拷贝、version 递增 v1.0→v2.0、previousQuoteId 指向旧版、重置审批 / 盖章节点、清空 sentAt；作废单历史版本保留不可删除（〔据代码现状还原〕，见 SYSTEM-OVERVIEW 3.4）。
- 数据模型 Quote 关联 leadId → contractId，打通「线索→报价→合同」漏斗；报价单编号 ZK-YYYYMMDD-NNN（〔据代码现状还原〕，见 PLAN 数据模型）。
- 旧文件删除 + 三旧路由 loader redirect 兼容；工作清单回写对齐（报价 90%）。
- 报价模块与线索侧旧报价口径（quotationPricing.ts 项目总价×上浮）并存两套口径（〔据代码现状还原〕，SYSTEM-OVERVIEW 已列疑点）。

#### 遗留与风险

- **报价模块遗留**（依据：开发记忆）：Excel 双向导入导出解析、版本 Diff 对比器、真实 PDF 生成与电子签章、代理 / 转交机制、超时催办接入、成本敏感数据的销售视图脱敏、Stage3/4 各步骤内容进一步细化。其中「真实 PDF 生成与电子签章」指正式文档产出与签章校验，仍属遗留；阶段 4 的「PDF 下载」是从简导出（打印预览 + 预设印章图片），两者不冲突。
- **范围外**（PLAN）：真实登录 / 权限系统（用 mock 角色切换替代）、真实电子签章 / 哈希校验（PDF 从简）、扣子（Coze）API 对接（功能清单用 Excel 导入 + 手动编辑模拟）。
- 两套报价口径并存（线索侧项目总价×上浮 vs 人天成本 TECH_DAILY_RATE=600），后续需收敛或明确边界（SYSTEM-OVERVIEW 疑点 5）。
- 已登记疑点：`Stage3WebAutomation.tsx` 命名含 WebAutomation 但内容是真实报价配置；`Stage3-WebAutomation-RULES.md` 描述已废弃的网页自动化；`quote_summarized` / `pending_followup` 为预留态，Context 无对应迁移函数（SYSTEM-OVERVIEW 疑点 1~3）。

（依据：PLAN.md（2026-08-14 锁定）/ 报价单流程规范.md / 报价单场景设计.md / 报价流程严格审查报告.md / 开发记忆（08-14 报价实施 + UI 优化两段）/ SYSTEM-OVERVIEW.md 3.4 / 兜底提交 58c53a0 与 1c27682；本阶段无独立提交，关键表述为〔据代码现状还原〕）

### 阶段 4：报价四个工作台 + 端平台架构（2026-08-15）

#### 背景与触发

08-15 会话对报价系统四个工作台做全面重构，引入「端 + 平台」架构。此前端选型、基础岗位配置等数据散落在工作台三，经 grilling 确认职责边界：端 + 平台选型归工作台一（功能清单）、基础岗位配置归工作台二（技术评估）、销售增项岗位与出差 / 驻场 / 其他成本归工作台三（报价配置）、审批盖章归工作台四。重构规模有提交可考：`58c53a0`（08-15 02:17，会话前快照）→ `1c27682`（08-16 07:37，会话后兜底提交）`quotation/` 目录共 15 文件变更、净 +2296/−890 行〔据代码现状〕。

#### 做了什么

- **量化规模**〔据代码现状〕（git diff `58c53a0`→`1c27682` 实测）：`quotation/` 15 文件、+2296/−890；四 Stage 各自 LOC 增长——Stage1FeatureList 381→685（+304）、Stage2EvalSheet 585→1062（+477）、Stage3WebAutomation 468→665（+197）、Stage4Approval 271→479（+208）；types.ts 427→453；RoleSwitcher.tsx 删除（−27）。
- **工作台一（功能清单）**：端列 rowspan 合并 + 平台 Tag 竖排，原生 HTML table 替换 Arco Table（Arco Table 不支持 rowspan）；端配置（新增端、端名称内联编辑、平台多选 Popover、新增模块 / 子功能级联菜单）；新增端 / 模块 / 子功能后焦点自动定位；底部浮动栏（端数 + 模块数 + 功能数）；大箭头阶段导航（QuotationWorkbench 壳）。
- **工作台二（人天评估）**：原生 table + rowspan 端列（只读）；表头冻结 sticky（手写 `position:'sticky'`，Stage2:623）；岗位删除（×）、拖拽排序（⠿ 手柄）、新增岗位（预设 + 自定义）；WASD 带值漫游（重写：此前仅移动焦点不填入数字，本次带值）+ 上下键调值（光标位置感知，0.1 / 1 / 10 / 100 档位，NumericInput / getStepFromCursor）；合并累加 / 解除合并寄存值机制；手动核定工期必填校验；底部浮动栏（定位按钮 + 岗位统计 + 端统计）。
- **工作台三（报价配置）**（Stage3 文件名仍为 Stage3WebAutomation）：本次新增「功能清单 + 工时评估」合并表（端 / 模块 rowspan + 各岗位工时列 + 人天列 + 合计行，此前 58c53a0 仅「技术人天评估」卡片〔据代码现状〕）；岗位日均成本可编辑（表头 ¥XX/天 点击修改、改后重算总金额）；出差 / 驻场由单地点改为多地点（travelDetails / onsiteDetails 数组）；销售增项岗位（名称 / 人数 / 天数 / 日均单价 / 事由 / 小计）、其他成本（费用项 + 合计）、报价汇总（项目综述 + 费用明细表）为沿用并随本次重排。
- **工作台四（审批盖章）**：基于工作台三数据结构展示（关系见关键决策）；三人并行会签 + 盖章节点；PDF 下载（html2canvas 截图 + jspdf 生成，带「中科集团公章」样式——〔据代码现状〕此前为占位 `Message.info('…占位')`，本次实现真实导出）。
- **其他**：报价卡片（LeadDetail 显示工期 / 人天 / 报价 / 端数 / 模块数 / 子功能数，作废单显示作废原因）；作废按钮（工作台顶部「流转轨迹」右侧，弹窗必填原因）；mock 数据合理化（用户端微信小程序 + 管理后台 PC Web 端；7 模块、20 子功能、14 评估单元；出差 2 地点、驻场 1 地点）。

#### 关键决策

- **「工作台一~四」与「Stage1~4」为 1:1 完整对应**〔据代码现状〕（QuotationWorkbench renderStage 为唯一渲染处）：工作台一 = Stage1FeatureList（UI 标题「工作台一 · 产品经理功能清单」）、工作台二 = Stage2EvalSheet（「工作台二 · 技术人天评估」）、工作台三 = Stage3WebAutomation（「工作台三 · 报价配置」）、工作台四 = Stage4Approval（「工作台四 · 管理层审批与盖章」）；STAGES=[1,2,3,4]，与 QUOTE_STAGE_NAMES（1 功能清单 / 2 人天评估 / 3 报价配置 / 4 审批盖章）一致。文档用「工作台」、代码用「Stage」，两者等价。
- **端 + 平台架构**：一个端适配多平台（EndpointConfig.platforms），端列 rowspan 合并为同端只显示一次端名 + 平台 Tag 竖排，解决「一端多平台」的表格表达。
- **WASD 键盘漫游 + 光标位置感知调值**：W / S / A / D 跨单元格带值漫游（fillRef 记录当前格）；上下键按光标位置增减 0.1 / 1 / 10 / 100（getStepFromCursor 依据 selectionStart）——键盘漫游为重写，此前「只移动焦点，不填入数字」（58c53a0:317 注释原文）〔据代码现状〕。
- **用原生 HTML table 实现 rowspan**：Arco Table 不支持 rowspan，故端列 / 模块列合并改用原生 `<table>` 手写 rowSpan；表头冻结 sticky 亦手写实现〔据代码现状〕。
- **工作台四「数据层面基于工作台三、组件不复用」**〔据代码现状〕：Stage4Approval 零引用 Stage3，复用同一 quote 数据（featureList / evalSheet / salesAddedRoles / travelOnsite / otherCosts / summary），但合并表为独立实现（自带 rowSpan + 只读渲染）。

#### 结构·数据变化

- **types.ts**（427→453〔据代码现状〕）：新增 `EndpointConfig`（id / name / platforms）、`PLATFORM_OPTIONS`（11 种平台：微信 / 支付宝 / 抖音小程序、iOS / Android / 鸿蒙 APP、H5 移动端、PC Web 端、桌面应用、iPad 端、Android 平板端）；`FeatureModule.endpointId` 关联端；`Quote.endpointConfigs` 端配置列表；`TravelOnsiteConfig` 由单地点（travelDetail / onsiteDetail）改为多地点数组（travelDetails / onsiteDetails）。
- **四个工作台职责重划**：端 + 平台选型从工作台三上移到工作台一；基础岗位配置留工作台二；销售增项 + 出差 / 驻场 + 其他成本归工作台三；审批盖章归工作台四（依据：SESSION-LOG 职责表）。
- **新增依赖**〔据代码现状〕：html2canvas@^1.4.1、jspdf@^4.2.1（声明于 package.json，供 PDF 导出使用）。
- **其他变化**〔据代码现状〕：mock 数据合理化；LeadDetail 报价卡片 + 作废原因展示；QuotationWorkbench 新增作废按钮（58c53a0 无）与 clipPath 大箭头阶段导航（58c53a0 为普通精简导航条，无箭头形状）。
- 会话内同提交的其他改动（1c27682 共 27 文件）：MainLayout / TodoCenter / ReminderBell / todos/types 与文档族（ARCHITECTURE / CONTEXT / PLAN / SYSTEM-OVERVIEW）同步更新。

#### 遗留与风险

- **上下键调值光标位置判断规则冲突**（SESSION-LOG 待解决 #1）〔据代码现状〕：「3.0」字符串（dotIdx=1）四个光标位置中 3 处与注释不符——注释（Stage2:43-44）称 pos0~pos3 = 百位(100) / 十位(10) / 个位(1) / 小数位(0.1)，而实现 getStepFromCursor 返回 十位(10) / 个位(1) / 小数位(0.1) / 小数位(0.1)：仅末位一致，前 3 位均错位。注释按「整数位恒含百 / 十 / 个」描述，实现按「距小数点的位数」计算，规则与实现未对齐，需二选一收敛。
- **端列 rowspan 原生 table 与 Arco Table 并存**（SESSION-LOG 待解决 #2）〔据代码现状〕：两套表格实现同模块并存——Stage1 / 2 / 3 渲染 0 个 Arco `<Table>`（全用原生 `<table>`），Stage4 同页混用 4 个 Arco Table（销售增项 / 出差 / 驻场 / 其他成本）+ 2 个原生 table（合并表 / 费用明细）；「合并表」原生实现被复制三处（Stage2 评估网格 / Stage3 合并表 / Stage4 只读重渲染），存在漂移风险；Stage1 仍遗留 Arco `Table` 死 import（import 无渲染）〔据代码现状〕。
- **工作台四合并表为独立实现 → 漂移风险**〔据代码现状〕：数据基于工作台三、组件不复用，Stage3 可编辑渲染与 Stage4 只读重渲染各自独立，后续结构变更需两处同步。
- 待补充确认：1c27682 提交覆盖 08-15 会话整体（含文档族等非报价改动），若后续需精确定位单功能，请回看该提交内各文件 mtime。

（依据：SESSION-LOG-2026-08-15.md + 开发记忆 08-15（zk-hubx-session-progress-2026-08-15）+ 代码核实〔据代码现状〕：git diff 58c53a0→1c27682 实测 15 文件 +2296/−890、四 Stage 与 types 各 LOC、Stage4 零 Stage3 引用、getStepFromCursor「3.0」光标位置、html2canvas / jspdf 依赖声明）

### 阶段 5：monorepo 重构 + α/β 双版本 + 报价域打通（2026-08-16 上午）

#### 背景与触发

阶段 4 结束时（兜底提交 `1c27682`，07:37），HubX 仍是单体前端原型：全部数据为页面内 useState + mock，无任何网络层。跨会话记忆已确立 α/β 双版本规划（α 版=纯前端、β 版=前后端），上午把结构落地：将 HubX 改造成 npm workspaces monorepo，让「同一份 UI」同时服务「纯前端原型」与「接真实后端的 β 版」，并以报价域为样板打通第一条前后端链路。触发提交 `865ff80`（09:04）「feat: monorepo 重构为 α/β 双版本，报价域打通前后端」，267 文件、+2843/−1335。

#### 做了什么

- **npm workspaces monorepo 落地**：HubX 变为 workspaces 根——`packages/ui`（@zkhubx/ui，唯一 UI 源）+ `apps/prototype`（@hubx/prototype，α 版）+ `apps/web`（@hubx/web，β 前端）+ `apps/api`（@hubx/api，β 后端）。
- **源码迁移**：旧 `src/` 迁到 `packages/ui/src/`（`src/app` → `packages/ui/src/app`）；两个前端 app 通过 vite 的 `@` alias（→ `packages/ui/src`）复用同一份 UI，改 `packages/ui` 后 α/β 同时生效。
- **报价域 Service 接口异步化**：新建 `packages/ui/src/services/quotationService.ts`——`QuotationService` 接口（`list()` / `createQuote()` 等，异步）+ `createMockQuotationService()`（内存 + localStorage）+ `createHttpQuotationService(baseUrl)`（fetch `/api/quotes`）。
- **抽共享 mutation 纯函数**：`quotationMutations.ts`（212 行）承载状态迁移 / 会签 / 版本逻辑，mock 与 http 共用，保证口径一致。
- **β 后端 Workers 骨架**：`apps/api/src/index.ts`（73 行）Hono 应用，内存 `Map` 存储 + 全开 CORS + 种子报价（`ZK-20260816-001`），提供 GET / PUT `/api/quotes`。
- **注入分叉**：`apps/web/src/main.tsx` 注入 `createHttpQuotationService(apiBaseUrl)`（baseUrl 用 `VITE_API_BASE_URL`，本地默认 `http://localhost:8787`）；α 版不注入、默认 mock。
- **报价域其他改造**（865ff80 提交说明）：全局模拟身份（`currentUser.ts` 定义 `CURRENT_LOGIN_USER` 张三 + 顶栏身份下拉替代 RoleSwitcher）、待办三处落地、销售身份统一张三、状态机死分支清理。
- **文档**：新建 `docs/ALPHA-BETA-ARCHITECTURE.md`（双版本 + monorepo + 数据接缝架构），ARCHITECTURE / CLAUDE / FRONTEND_CONVENTIONS 路径同步更新。

#### 关键决策

- **服务接缝模式作为后续模块样板**：UI 不直接读写数据，依赖 `services/` 里的 Service 接口，数据来源由每个 app 注入——架构文档给出五步样板步骤（定义接口 + mock/http 双实现 → 抽共享 mutation → Context 注入 service + 异步加载 → 后端路由 → 前端注入），供合同 / 线索等域照抄。
- **业务逻辑抽共享 mutation 纯函数**：状态迁移 / 会签 / 版本等抽到 `quotationMutations.ts`，mock 与 http 共用一份，保证前后端口径一致。
- **α 版保 mock、β 版接 http**：两个前端只差「注入什么 service」，UI 源码完全同一份，α 版继续可离线演示。
- **版本命名定型**：α 版=纯前端（prototype）、β 版=前后端（web+api），都部署 Cloudflare。

#### 结构·数据变化

- HubX 从单体目录变为 npm workspaces：`packages/ui` + `apps/prototype` / `apps/web` / `apps/api`；根 `package.json` 增 workspaces 配置。
- `services/` 数据访问层目录建立（`quotationService.ts` / `quotationMutations.ts`），成为「数据接缝」所在地。
- `QuotationContext` 接受 `service` prop（`App.tsx` 透传 `quotationService`）：α 版不传（默认 mock）、β 版传 http；Context 由 401 行重写为 279 行。
- 数据层新增第一条真实网络路径：β 前端 fetch → Workers `/api/quotes`（内存存储）。

#### 遗留与风险

- **仅报价域打通**：线索 / 待办 / 合同等其余域仍是 mock，尚未抽 service 接缝、未接 http。
- **β 后端为内存存储**：重启即重置、无持久化；未做状态迁移校验、无 DELETE 端点（阶段 6 补齐）。
- 本地联调注意：β 前端 5174、α 版 5173 各自 dev，β 后端需单独 `npx wrangler dev`（8787）起。

（依据：865ff80「feat: monorepo 重构为 α/β 双版本，报价域打通前后端」（2026-08-16 09:04）/ ALPHA-BETA-ARCHITECTURE.md / 开发记忆 zk-hubx-session-progress-2026-08-16）

### 阶段 6：部署 + 合同域打通（2026-08-16 下午）

#### 背景与触发

上午完成 monorepo 与报价域打通后，下午（自 `9eda9d0` 14:47 起）进入「跑通 + 落地」：把 α/β 三端部署到 Cloudflare、把 β 后端从内存存储升级为 D1 持久化，并按报价域样板把合同域接上 http。下午共 7 笔提交，14:47 → 15:16 约半小时内完成（阶段 5 收于上午 `865ff80` 09:04，中间为午间间歇）。

#### 做了什么

- **Cloudflare 三端部署**（9eda9d0，14:47）：β 后端 Workers（`zkhubx-api.llhfire.workers.dev`）+ β 前端 Pages（`zkhubx-web.pages.dev`）+ α 版 Pages（`zkhubx-alpha.pages.dev`）；α 版新增 `public/_redirects`（SPA 深链接 `/* /index.html 200`）；新建 `DEPLOYMENT.md` 部署指南（78 行：一次性登录 / β 后端 / β 前端（构建期内联 `VITE_API_BASE_URL`）/ α 版 / 常见问题 / 建议下一步）。
- **β 后端接 D1 持久化**（ded813d，14:54）：`schema.sql`（quotes 表）+ `wrangler.toml` 配 D1（库名 `zkhubx-db`）+ `index.ts` 从内存 `Map` 改 D1（Cloudflare SQLite）读写；`09d922f`（14:54）同步更新部署指南（「D1 已接」）。
- **服务端状态迁移校验 + DELETE 端点**（1a585cb，14:59）：`index.ts` +44 行——报价状态迁移合法性校验 + DELETE `/api/quotes/:id`。
- **合同域抽 service 接缝**（5aa2999，15:06）：新建 `packages/ui/src/services/contractService.ts`（357 行）；`ContractsContext.tsx` 大瘦身（835 行 → 约百行，改由 service 提供数据）；`ContractWizard` / `LeadContractHistoryPanel` 接线。
- **合同域接 http + 抽共享 mutation**（bc21304，15:15）：新建 `contractMutations.ts`（260 行，mock/http 共用）；`contractService.ts` 重构为 mock/http 双实现；后端 `schema.sql` +6 行（合同表）、`index.ts` +23 行（合同端点）；`apps/web/src/main.tsx` 注入 http、`App.tsx` 透传 `contractService`。
- **架构进度更新**（6ebc5fa，15:16）：`ALPHA-BETA-ARCHITECTURE.md` 更新为「报价 / 合同域 http 全通 + 部署完成」，待办剩三项（其余域接缝 / 合同域状态迁移校验 / 自定义域名）。

#### 关键决策

- **合同域照报价域样板**：Service 接口 + mock/http 双实现 + 共享 mutation 纯函数 + Context 注入 service，五步样板完整复刻（架构文档明示「给一个新模块加接缝的步骤，报价域已完成，作样板」）。
- **D1 持久化选型**：β 后端从内存 `Map` 升级 Cloudflare D1（SQLite），重启不丢、生产可用；首次部署需初始化 D1（`wrangler d1 create` + `d1 execute --file schema.sql --remote`）。
- **部署地址定型**：β 后端 `https://zkhubx-api.llhfire.workers.dev`、β 前端 `https://zkhubx-web.pages.dev`、α 版 `https://zkhubx-alpha.pages.dev`；β 前端在构建期把 API 地址内联进产物。

#### 结构·数据变化

- `apps/api` 从报价单路由扩展为多端点：quotes CRUD（GET / PUT / DELETE + 状态迁移校验）+ 合同端点；`schema.sql` 新增合同表（D1）。
- `services/` 新增 `contractService.ts` / `contractMutations.ts`，与 `quotationService.ts` / `quotationMutations.ts` 并列；`ContractsContext` 改「注入 service + 异步加载」。
- 两个前端 app：`apps/web` 注入 http（`VITE_API_BASE_URL` 指向 workers 地址）、`apps/prototype` 保持 mock；`App.tsx` 同时接受 `quotationService` / `contractService` prop 透传。
- 部署产物：Cloudflare 三实例上线（Workers ×1 + Pages ×2），β 后端数据持久化到 D1。

#### 遗留与风险

- **线索 / 待办等其余域仍 mock**（架构文档待办 1）：尚未抽 service 接缝，需照合同 / 报价样板继续。
- **合同域服务端状态迁移校验待补**（架构文档待办 2）：前端已共用 mutation 校验，后端需照报价样板补服务端校验。
- **自定义域名待做**（架构文档待办 3）：`*.workers.dev` / `*.pages.dev` 国内直连被墙（GFW），需走代理 / VPN 访问，长期需备案域名或换国内托管。
- 本地联调注意：`npx wrangler dev` 为内存态，重启即重置、别手动 PUT 不完整字段（前端 `computeAmountBreakdown` 会崩）；线上 D1 持久化不受影响。

（依据：提交 9eda9d0 / ded813d / 09d922f / 1a585cb / 5aa2999 / bc21304 / 6ebc5fa（2026-08-16 14:47~15:16）/ DEPLOYMENT.md / ALPHA-BETA-ARCHITECTURE.md / 开发记忆 zk-hubx-session-progress-2026-08-16）

> 阶段 6 之后为「当前状态快照」与「附录」（git 提交清单）章节，由后续任务追加。

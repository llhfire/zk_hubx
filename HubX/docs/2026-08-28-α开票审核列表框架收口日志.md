# 2026-08-28 α 开票审核列表框架收口日志

## 本次范围

仅调整 α 开票审核一级列表、筛选纯函数与相关说明文档，未修改 β Service、Workers 或 D1 链路。

## 已完成

- `ProjectInvoicePage` 复用 `PageShell`、`PageHeader`、`FilterBar` 和统一指标网格，一级列表保持无面包屑。
- 页头统一为 20px 标题与范围说明；新增申请总数、待开票、已开票、已冲红四项任务摘要。
- 保留全部、待开票、已开票、已冲红状态 Tab，并新增项目编号、项目名称、客户、期次和发票类别真实检索。
- 补齐结果数量、筛选重置和区分“无申请/无匹配”的空状态；数值右对齐，操作按钮增加可访问名称。
- 保留客户开票资料复制、项目回款期次核对、发票附件下载与待开票上传确认流程。
- 规划中的按项目、客户和时间段汇总统计未提前实现，继续保留在功能看板规划项。

## α 边界

开票申请继续读取 `ProjectInvoiceContext` 的 α 浏览器本地数据。服务端查询、分页、附件存储与刷新持久化属于 β 接线，本次未扩展。

## 验证

- 开票列表筛选与摘要纯函数新增 4 项 Vitest，覆盖多字段关键词、状态组合、指标计算和筛选识别。
- 1440×1000 浏览器验收通过：空数据说明清晰；三条隔离样本可按项目、客户和状态筛选，详情与处理弹窗均正常。
- 760px 窄屏无页面级横向溢出，指标自动双列排列，搜索框全宽，表格保留内部横向浏览。
- 全量 Vitest 通过：82 个测试文件、968 项测试全部通过；α 生产构建通过。

## 后续

继续 α 全页面一致性计划，下一项优先核对系统配置一级列表，再批量补齐二级以上页面面包屑。

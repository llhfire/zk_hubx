# 下班交接

> 会话上下班交接文档（固定单份）。下班时覆盖写最新状态；上班时先读此文件恢复上下文。
> 更新时间：2026-08-18 晚

## 本次进度（本次会话做了什么）

- **功能看板全面升级**：
  - 从 14 模块扩展为 **36 模块**（对齐 `ZK-HubX架构图.html` 8 域：销售/交付/财务/获客/支撑/跨域/资源/运维）
  - 领域按优先级排列：销售→交付→财务→获客→支撑→跨域→资源→运维
  - 新增 `isPlanned` 字段区分现有/规划模块，规划模块虚线边框+半透明+'规划'标签
  - **已有功能按代码核查修正**：94→67 项（砍掉无代码实现的 27 项，每个描述标注实际文件名）
  - **待设计功能扩充**：77→105 项（移入无代码项+原有规划项）
  - 待设计功能状态从文字 Tag 改为**彩色圆点图标**（灰=未开始/橙=已调研/紫=设计中/绿=已设计）
- **功能看板功能列表列重构**：
  - 「覆盖范围」→「已有功能」：文字块矩阵，点击弹出功能说明 Modal（原始需求/流程/说明）
  - 「功能列表」→「待设计功能」
  - 新增 `ExistingFeature` 类型 + `features` 字段（`featureBoard.config.json` 唯一事实源）
- **P0 ② 回款权限矩阵（WIP）**：上次会话的 WIP 提交（`c37acf7`）未动，待下次收尾
- **线上地址**：`alpha.zkhubx.com` / `beta.zkhubx.com`（上次会话完成）

## 当前状态要点

- 架构：monorepo（packages/ui + apps/prototype α版 + apps/web β前端 + apps/api β后端 Workers+D1）
- 已通 HTTP：报价域、合同域；其余域仍 mock
- 部署：β后端 `zkhubx-api.llhfire.workers.dev`；前端 `alpha.zkhubx.com` / `beta.zkhubx.com`
- 功能看板：36 模块 / 67 已有功能 / 105 待设计功能 / 8 领域优先级排列
- 本地 dev：`npm run dev -w apps/prototype` → `localhost:5173`

## 下次待办（按优先级）

1. **P0 ② 回款拆分/冲红权限矩阵收尾**：矩阵函数与 UI 接线已完成（WIP `c37acf7`），待补单测 + 构建验证 + 演进史 P0 ② 标完成
2. **P0 ③ 财务全期次回款 + 开票视图**：一屏查看项目全期次回款与开票状态
3. **清测试债**：`projectTasks.test` / `workAttribution.test` / `dailyReportModalInit.test` 3 个断言失败 + `components.test` / `ProjectDetail.test` react-quill jsdom 崩溃
4. 可选：合同域服务端状态迁移校验（照报价域样板）

## 踩坑提醒

- vitest 需在 `HubX/apps/prototype` 目录下跑
- `featureBoard.config.json` 的 `features` 和 `planned` 数据由种子生成，修改需同步改 `featureBoardModel.ts` 的 `FEATURES_SEED` / `PLANNED_SEED`
- 规划模块（`isPlanned: true`）在 UI 中虚线+半透明，不影响现有模块
- 状态圆点点击弹出下拉切换（不是直接 toggle），颜色：灰/橙/紫/绿
- `alphaChecklist.config.json` 已废弃，勿再引用

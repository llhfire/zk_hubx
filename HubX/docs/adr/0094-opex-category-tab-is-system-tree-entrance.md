# 运营费用第五 Tab 是系统费用分类入口，不是第二套科目树

财务要在运营费用里看见科目，所以加第五 Tab。它读写系统费用分类已有的那一棵两级树：可录入八个一级不变，`LABOR` 只读且不可录入。不在本模块另存字典，不把脱敏做成科目属性，不开放任意新增一级（避免 `MARKETING` / `IT_SERVICE` 回流）。这修订 ADR-0083「不要 Tab 5」的导航范围，不修订 ADR-0078「全公司只有一套科目」。

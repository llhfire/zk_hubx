# 2026-08-28 α 智能会议长流程框架收口日志

## 本次范围

仅调整 α 智能会议工作台、行动项到待办中心的前端接缝及 α 权限规则，未修改 β Service、Workers 或 D1 链路。

## 已完成

- `SmartMeetingWorkbench` 接入 `PageShell`、`ProcessOverview`、`ProcessMetricGrid` 和 `ProcessWorkspace`。
- 增加“智能会议 / 纪要列表 / 当前纪要”三级面包屑、4 步流程概览和 6 维摘要指标。
- 工作主体收口为 70:30；主区按会议整理、纪要内容、行动事项三个工作区组织，侧栏集中当前任务、会议角色、业务关联、提交检查和行动项摘要。
- 保留文本导入、元信息、核心决议、正文、行动事项、版本抽屉、提交确认、确认同步和归档能力。
- 修复新建纪要提交时仍使用临时 ID 的问题；创建后会使用真实纪要 ID 进入待确认状态并跳转详情。
- 修复确认流程调用旧待办接口的问题，补齐待办创建、更新与软取消；行动项同步继续保持 `actionItemId` 幂等。
- 统一权限规则：已确认和已归档纪要对整理人及管理员均为只读。

## α 边界

纪要与待办仍使用 α 的浏览器本地持久化；β D1 表、服务端不变量校验和 AI Secret 不在本次范围。

## 验证

- 智能会议定向 Vitest 全部通过。
- α 全量测试与构建通过。
- 1440×1000 真实页面验收：4 步流程、6 项指标、70:30 主辅区及三个工作区完整，无水平溢出。
- 浏览器实测“新建纪要 → 提交确认 → 跳转新 ID → 待确认”、确认后行动项同步待办、列表返回状态保持和确认后只读均正常，控制台无报错；测试数据已恢复。

## 后续

首批 α 长流程页面迁移完成。下一阶段继续全页面一致性计划，优先批量统一一级列表壳层、二级以上页面面包屑和标题尺度。

# 2026-08-28 α 交付计划长流程框架收口日志

## 本次范围

仅调整 `apps/prototype` 使用的交付计划体验，未扩展 β 数据链路。

## 已完成

- `DeliveryPlanPage` 接入 `PageShell`、`ProcessOverview`、`ProcessMetricGrid` 和 `ProcessWorkspace`。
- 增加“项目管理 / 项目 / 交付计划”三级面包屑、7 步 SOP 概览和 6 维交付指标。
- 工作主体收口为 70:30；主区保留任务清单/甘特图同步，侧栏集中当前阶段、计划上下文与图例。
- 板块筛选和日/周/月粒度收入单一工具栏；任务行固定 40px，避免与甘特行错位。
- 交付计划配置、步骤编辑、自定义步骤和删除改走 `deliveryPlanStore`，跨页状态不再只停留在当前组件。
- 修正交付种子步骤与板块状态不一致，顶部正确定位到第 3 板块。

## 验证

- 交付域 Vitest：60 项原有纯函数测试通过，并新增种子状态和共享存储回归。
- α 构建通过。
- 1440×1000 真实页面验收：无水平溢出，主区占比 70%，板块筛选、时间粒度和步骤编辑弹窗可用，控制台无报错。

## 后续

继续只做 α 长流程页面迁移，下一页为 `TripDetail`。

# 下班交接

> 会话上下班交接文档（固定单份）。下班时覆盖写最新状态；上班时先读此文件恢复上下文。
> 更新时间：2026-08-16 晚

## 本次进度（上次会话做了什么）

- **演进史文档**：根目录《演进史.md》五章建立（需求采集→当前 7 阶段），git 提交清单、P0 现状快照齐全。
- **GitHub 备份**：19 提交全部 push，之后每次改动均同步 push（GitHub 仅备份，部署仍走本机 wrangler）。
- **P0 ① 合同审批单节点收敛** ✅：`createInitialApprovalFlow()` / `applySubmitVersionForApproval()` 默认值 → 总经理单节点；9 条 mock 合同审批流、补充协议初始化、侧面板 prop 默认全部收敛（保留 standard 显式模式）。4 个 commit：`e2c141d` `529eb45` `416e129` + 演进史 `cd7999d`。
- **上下班规则**：已定义并写入记忆（见记忆 `zk-hubx-workday-rules`）。

## 当前状态要点

- 架构：monorepo（packages/ui + apps/prototype α版 + apps/web β前端 + apps/api β后端 Workers+D1）。
- 已通 http：报价域、合同域；仍 mock：线索 / 待办等其余域。
- 部署：本机 wrangler CLI 直推，GitHub 仅备份不参与构建。

## 下次待办（按优先级）

1. **P0 ② 回款拆分 / 冲红权限矩阵**（四种回款×开票状态矩阵）。
2. **P0 ③ 财务全期次回款 + 开票视图**（全期次矩阵挂到财务路由）。
3. **清测试债**：`projectTasks.test` / `workAttribution.test` / `dailyReportModalInit.test` 3 个断言失败 + `components.test` / `ProjectDetail.test` react-quill jsdom 崩溃 —— monorepo（865ff80）迁移遗留，与本次改动无关。
4. 可选：合同域服务端状态迁移校验（照报价域样板）、绑自定义域名。

## 踩坑提醒

- vitest 需在 `HubX/apps/prototype` 目录下跑（`cd apps/prototype && npx vitest run <文件>`），根目录直跑不解析 `@` alias。
- 合同域 `'standard'` 四步模式仍保留（`createInitialApprovalFlow('standard')` 显式可调），勿误删。

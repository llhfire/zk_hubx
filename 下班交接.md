# 下班交接

> 会话上下班交接文档（固定单份）。上班时先读此文件恢复上下文。
> **下班 = 写本文件 + 有代码改动才发布 Cloudflare**（用户说「下班」即触发，不要再确认）。发布步骤见 `HubX/docs/DEPLOYMENT.md`「下班发布」。
> 更新时间：2026-08-19（下班：两轮 grill 收束——精益交付数据链路 + 差旅管理完整化，全部文档落地，业务页面未改）

## 本次进度（本次会话做了什么）

**规划日，零业务页面编码。两轮 /grill-with-docs 收束，三份计划排队待开工。**

### 第一轮：精益交付数据链路收束（+ 严格复审补丁）

审计发现移植稿全是硬编码矛盾（totalCost≠明细、报价三口径打架、contractId 查无此合同、回款=合同额×1.5、EAC 四个值）。锁定 10 决策 + 6 推论，又经严格审查补 6 项修正（R1-R6）。核心：

- 派生计算模式：mock 只造原子事实，一切汇总派生（calc.ts）
- 跨域引用：报价/评估读报价域，合同/回款读合同域，删平行 mock
- 收入口径=回款；**指标双口径**：全周期利润率（标的额 vs EAC）管健康灯，回款口径只管 WIP
- Case 承认**轻实体**：持有 targetMargin/budgetCap/commercialCap + 自有 10 态状态机
- 成本三通道写入口（日报/报销/公摊自动 + manual 兜底）；模拟器敏感性+底线价（只缩 forecast 人力）；predictedPnl 四类快照
- 保留 CASE- 独立编号，主 CONTEXT.md 业务单改**四视角**（财务交付视角）

### 第二轮：差旅管理完整化

代码现状比精益交付健康（travel-api 接缝、8 页面、无 TODO），但有 mock/类型脱节（mockPunchRule 缺字段、mockExpenseStandards 结构完全不对、无 tsc 护栏）、三套城市分级、mock 极薄、跨域断链。锁定 8 决策 + 10 推论：

- 核心链优先；**打卡模块整体删除**（T1）
- 一级菜单 + 看板/架构图归财务域
- 差旅单**必挂项目或线索**（ADR-0089，已修订主 CONTEXT.md 费用科目「差旅必填项目」旧表述）
- 宿舍费用**账留宿舍模块**（ADR-0090，运营费用只读引用合计）
- 审批固定流 + 借款金额分级；超标硬软分档；单一补贴（城市分级 ×（自然日−出发日−返回日））；借款最早未结顺序冲抵；报销双出口

### 落盘文档（本次全部）

- PRD：`文档/PRD/PRD-精益交付数据链路.md`、`文档/PRD/PRD-差旅管理.md`
- 计划：`计划/当前/lean-delivery-dataline-dev-plan.md`（L1-L4）、`计划/当前/travel-dev-plan.md`（T1-T4）
- ADR：0086/0087/0088（精益交付）、0089/0090（差旅）
- 术语：主 `HubX/CONTEXT.md`（业务单四视角 + 5 条差旅术语 + 费用科目差旅表述修订）；`pages/financial-delivery/CONTEXT.md` 重写（原错位为运营成本费用管理）
- 看板：财务域新增「精益交付」「差旅管理」两模块（37->39），featureBoardModel 测试期望 36->39（上轮遗留的 36 旧断言一并修），10/10 绿
- 架构图：财务域 5->7 模块
- 代码改动仅看板三件（config/model/test），业务页面零改动

## 当前状态要点

- 架构：单一 git 仓库；monorepo 在 `HubX/`
- 已通 HTTP：报价域、合同域；项目/商机/交付计划/精益交付/差旅仍是内存 mock
- 部署：α/β 随本次提交发布；`apps/api` 无改动，不发 Workers
- git：`main`，本次提交后 push
- 本地 α：`http://localhost:5173/`（后台运行中，任务 bguw012t2）
- 三份计划排队待开工：运营费用阶段 A（最早）、精益交付 L1-L4、差旅 T1-T4

## 下次待办（按优先级）

1. **开工任一线**（建议顺序）：
   - 运营费用阶段 A（`计划/当前/operating-expense-dev-plan.md`，最早计划）
   - 精益交付 L1（calc.ts 纯函数起步，无 UI 风险）
   - 差旅 T1（删打卡 + mock 类型护栏单测，机械性强）
2. 三线接缝约定（谁先落地谁定义）：公摊率常量 `OVERHEAD_RATE`（运营费用↔精益交付）；报销双出口（差旅↔精益交付 R3↔运营费用 TRAVEL 科目）
3. P0② 回款权限矩阵单测 / 基线债（长期靠后）

## 踩坑提醒

- **精益交付阶段 A 注意**：Case 是轻实体（不是零事实）——targetMargin/budgetCap/commercialCap + 自有状态机在 Case 上；健康灯必须用全周期口径，不能用回款口径利润率。
- **差旅 T1 删打卡要删干净**：页面/路由/菜单/types 打卡段/travel-api 打卡函数/mock 全链路，验收口径写了「无残留」。
- **仓库无 tsconfig**：vite build 不做类型检查，mock 与 types 脱节会静默吞掉（差旅已中招）——T1 的类型护栏单测是解药，精益交付 L2 重造 mock 时同样适用。
- **主 CONTEXT.md 的箭头是 U+2192「->」不是 ASCII「->」**：Edit 工具按 ASCII 匹配会失败，用 python 替换或带上正确字符。
- **单测在 `HubX/apps/prototype` 下跑**，从 HubX 根跑 vitest 会因路径别名失败。
- **下班发布三端清单制**：α 必发 / β 必发 / Workers 仅 `apps/api` 有改才发。核对 `wrangler pages deployment list` 的 Source SHA = HEAD。
- 差旅补贴天数 = 自然日 − 出发日 − 返回日（用户明确路途日不算）；mock 里 trip-1 的 7 天 × 150=1050 要改成 5 天 750。

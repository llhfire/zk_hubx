# 下班交接

> 更新时间：2026-08-20（下班：线索详情 360 恢复报价工作台 + 合同向导）

## 本次会话完成

### 线索详情 360：报价入口修复

- 根因：重构把「新建报价」改成 `navigate('/quotation')`，报价中心不能从线索建单，流程断开。
- 恢复旧详情页逻辑：
  - 「新建报价」调用 `createQuote`，全屏抽屉打开 `QuotationWorkbench`（功能清单 → 人天评估 → 报价配置 → 审批盖章）
  - 报价列表改为 `useQuotation()` 按 `leadId` 过滤的真实报价单
  - 卡片提供「进入工作台」；已作废报价单独展示
- 浏览器验证：`/leads/5940` 报价页签新建后停在线索详情，抽屉标题「报价工作台」，关闭后再点「进入工作台」可重开。

### 线索详情 360：合同入口修复

- 根因：同样被改成 `navigate('/contracts')`，只进列表，线索/报价预填丢失。
- 恢复旧详情页逻辑：
  - 「新建合同」进入 `/contracts/new?leadId=&returnTo=lead&from=`，并带 `leadContractPrefill`
  - 带最近一份已审核报价（`flowStatus === '已审核' && status === '已报价'`）
  - 从向导返回时按 `location.state` 切回「合同记录」页签
  - 真实合同卡片可点进合同详情
- 浏览器验证：向导预填客户「北京科技有限公司」、联系人「张经理」、金额 680000；「下一步」生成草稿并进入合同编辑页。

## 全量状态

| 项目 | 数据 |
|---|---|
| 线索列表页 | 5个 ✅ |
| 线索详情页 | 360° ✅（报价工作台 / 合同向导入口已恢复） |
| 项目列表页 | ✅ |
| 项目详情页 | 5域工作台 ✅ |
| Git | 待本班次提交推送 |

## 下次待办

1. **云发测试**：检查线索详情页和项目详情页的 UI 一致性
2. **详情页联动**：项目详情中关联线索的跟进/报价/合同信息联动
3. **权限模块**：数据范围权限（管理员/普通用户）的完善
4. **Bug 修复**：12 个前端 bug 中的后端相关项（TODO 标记）

## 踩坑

- 360 重构不要把「新建报价 / 新建合同」改成跳转列表。报价必须 `createQuote` + 抽屉工作台；合同必须 `/contracts/new` + `leadContractPrefill`。
- 报价中心底部「从线索发起报价」会再跳回线索列表，不能当建单入口。
- 默认报价角色是销售。新建草稿处于功能清单阶段，工作台会提示只读，需顶栏切到产品经理才能编辑。
- 合同向导的「下一步」会直接创建草稿并进入编辑页，不是多步表单里的下一页。

# 2026-08-28 α 业务单详情长流程框架收口日志

## 本次范围

仅调整 α 业务单详情页、经营参数口径与 mock 交互，未扩展 β 数据链路。

## 已完成

- `CaseDetail` 接入 `PageShell`、`ProcessOverview`、`ProcessMetricGrid` 和 `ProcessWorkspace`。
- 增加“精益交付 / 业务单管理 / 当前业务单”三级面包屑，顶部按当前真实路径展示生命周期，挂起和终止仍保持分支语义。
- 统一 6 维经营指标：有效标的额、已发生成本、EAC、累计回款、全周期利润率和 WIP。
- 工作主体收口为 70:30；主区保留经营概览、工时评估、报价单、成本归集和项目决算，侧栏集中当前任务、关联对象、风险参数与时间信息。
- 恢复收入与成本趋势图；成本结构继续支持“已发生 / 含预测”切换。
- 状态推进和管理参数保存会写回 α mock 数据，同一应用会话内离开页面再返回时保持一致。
- 修正目标利润率口径：管理参数中的 `30` 按 `30%` 参与健康状态计算。

## α 边界

状态与管理参数仍保存在当前前端模块内。浏览器硬刷新后会恢复种子数据；真实刷新持久化属于 β Service 与 D1 接缝，本次未扩展。

## 验证

- 精益交付定向 Vitest：2 个文件、38 项测试通过。
- α 构建通过。
- 1440×1000 真实页面验收：主区 813px、侧栏 348px，无水平溢出；8 个主路径阶段、6 项指标和 5 个页签完整。
- 参数由 30% 调整为 15% 后健康状态由“风险”更新为“预警”；状态推进至“验收中”后，经面包屑返回列表再进入详情仍保持，控制台无报错。

## 后续

继续只做 α 长流程页面迁移，下一页为 `SmartMeetingWorkbench`。

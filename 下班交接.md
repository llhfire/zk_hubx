# 下班交接

> 会话上下班交接文档（固定单份）。上班时先读此文件恢复上下文。
> **下班 = 写本文件 + 有代码改动才发布 Cloudflare**（用户说「下班」即触发，不要再确认）。发布步骤见 `HubX/docs/DEPLOYMENT.md`「下班发布」。
> 更新时间：2026-08-19（下班：差旅管理 + 精益交付模块移植）

## 本次进度（本次会话做了什么）

**差旅管理 + 精益交付模块从源仓库完整移植。**

1. **差旅管理模块移植**（从 llhfire/hubX 仓库）：
   - 数据层：types.ts、mock-data.ts、travel-api.ts、index.ts
   - 页面组件：TripList、TripForm、TripDetail（含 6 个子组件）、ReimbursementList、LoanList、TravelDashboard、StandardList、DormitoryManagement、PunchClock
   - 共享组件：ComplianceGuide、ExpenseOCR、FinanceAuditDashboard、TravelRuleEngine
   - 路由配置：9 条路由（/travel/*）
   - 侧边栏菜单：差旅管理一级菜单，7 个子菜单

2. **精益交付模块移植**（从 llhfire/hubX 仓库）：
   - 数据层：mockData.ts、CONTEXT.md
   - 页面组件：Dashboard（财务仪表盘）、CaseList（业务单管理）、CaseDetail（业务单详情）
   - 路由配置：11 条路由（/financial-delivery/*）
   - 侧边栏菜单：精益交付一级菜单（带 New 标签），5 个子菜单

3. **CaseDetail.tsx 完整功能实现**：
   - 概览 Tab：基本信息、财务指标、预测指标、成本与收入趋势图（含收款分析）、利润率趋势图（含利润率分析）、成本结构饼图、成本构成明细（含预测对比）、成本增长曲线
   - 工时评估 Tab：功能清单列表、展开收起、变更说明
   - 报价单 Tab：报价单列表、展开收起、变更说明
   - 成本归集 Tab：成本汇总、成本明细表格
   - 项目决算 Tab：根因分析、经验教训、效率指标
   - 财务模型详情弹窗

4. **组件库转换**：
   - 所有组件从 shadcn/ui 转换为 Arco Design
   - 图标从 lucide-react 转换为 @arco-design/web-react/icon
   - 安装 recharts 和 react-markdown 依赖

5. **页面布局优化**：
   - 统一卡片间距（gap: 16）
   - 同行卡片高度对齐（height: 100%）
   - 图例间距统一（16px）
   - 分析说明行高优化（lineHeight: 1.8）

## 当前状态要点

- 架构：单一 git 仓库；monorepo 在 `HubX/`
- 已通 HTTP：报价域、合同域；项目/商机/交付计划仍是内存 mock
- 部署：`alpha.zkhubx.com` / `beta.zkhubx.com` / `zkhubx-api.llhfire.workers.dev`，**三端线上均 = 191ef83（上次 HEAD），本次有代码改动未提交未发布**
- git：`main` 分支，有未提交改动（本次所有改动）
- 本地 α dev server：`http://localhost:5173/` 在跑（后台）
- 测试：339 通过 + 4 基线债
- `计划/当前/` 有 8 份 dev-plan，其中 unified-view U1/U4/U5/U6、报价 4.1/4.2 已完成编码

## 下次待办（按优先级）

1. **发布**：本次有代码改动，下班需发三端（α 前端 / β 前端 / Workers 有改动吗？需确认）。
2. **浏览器人工验证**：差旅管理各页面、精益交付各页面功能完整性。
3. **继续开发**：U3（作废↔搁置）或其他报价阶段。
4. **P0② 回款权限矩阵单测** / 基线债清偿。
5. **4 项未立项结构债登记进功能看板**（客户域统一等需要先 grill）。

## 踩坑提醒

- **下班发布三端清单制**：α 前端 / β 前端 / Workers（apps/api 有改才发），逐项打勾。发布后用 `wrangler pages deployment list`（前端）+ `wrangler deployments list`（Workers）双查 SHA。
- **Workers 部署必须在 `HubX/apps/api` 目录下执行** `npx wrangler deploy`。
- **报价域测试工作目录**：`HubX/apps/prototype`，从 HubX 根跑 vitest 会因路径别名解析失败。
- **Edit 工具对个别特殊字符（如 ≠）可能匹配失败**：换个不含特殊字符的锚点即可。
- **迁移函数 `migrateQuote` 是读时迁移**：旧 localStorage / D1 数据读取时自动转新词表，不主动写回。
- **`salesOwnerName` 是报价域字段**：不影响线索域的负责人（那是 leadOwnerName，不同字段）。
- **`canViewQuote` 五类人并集**：创建人 / salesOwnerName / 评估人 / 快照会签人 / 盖章人 / admin。ccSalesNames 里的抄送人不算。
- **Arco Design 图标名称**：IconClock → IconClockCircle、IconPrinter → IconPrinter（不是 IconPrinterer）、IconBuilding → IconHome 等，需注意拼写。

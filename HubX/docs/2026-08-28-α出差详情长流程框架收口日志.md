# 2026-08-28 α 出差详情长流程框架收口日志

## 本次范围

仅调整 α 出差详情页及 mock 状态流转，未扩展 β 数据链路。

## 已完成

- `TripDetail` 接入 `PageShell`、`ProcessOverview`、`ProcessMetricGrid` 和 `ProcessWorkspace`。
- 增加“差旅管理 / 出差申请 / 当前出差单”三级面包屑、6 步流程概览和 6 维摘要指标。
- 工作主体收口为 70:30；主区保留基本信息、旅程、费用、报销、借款和补贴六个页签，侧栏集中当前任务、关联对象、行程和出差事由。
- 已审批状态继续展示出差合规提示；页签切换会同步侧栏的当前工作区。
- 开始出差、结束出差和关闭出差会写回 α mock 数据；同一应用会话内离开页面再返回时状态保持一致。
- 新增 mock 生命周期回归测试，覆盖创建、提交、审批、开始、结束、关闭和删除。

## α 边界

mock 数据仅保存在当前前端模块内。浏览器硬刷新后仍会恢复种子数据；真实刷新持久化属于 β Service 与 D1 接缝，本次未扩展。

## 验证

- 差旅域 Vitest：4 个文件、33 项测试通过。
- α 构建通过。
- 1440×1000 真实页面验收：无水平溢出，主区占比 70%，6 个指标和 6 个页签完整；状态推进后站内返回仍为“进行中”，控制台无报错。

## 后续

继续只做 α 长流程页面迁移，下一页为 `CaseDetail`。

# 下班交接

> 更新时间：2026-08-25（下班：编码+数据迁移+β接线）

## 本次进度

### 1. 线索派发全部阶段完成（0→B→C→D→E）

**阶段 C（派发弹窗+催办+批量派发）**：
- `components/DispatchModal.tsx`：选部门→选销售或公海，支持单条/批量
- `LeadDispatchPage.tsx`：接入 DispatchModal，替换占位 handler
- 催办动作：appendLeadEvent urge + Message 提示
- 批量派发：checkbox 勾选 → DispatchModal 统一目标

**阶段 D（等级调整+质检）**：
- `components/LevelAdjustModal.tsx`：S/A/B/C 四级，升级免审/降级走审批
- `components/LeadTable.tsx`：操作列加「调级」「质检」按钮
- 等级调整：升级直接生效 + customerLevel 变更；降级写 level_change 事件进审核队列
- 质检确认：3 人退回 → 管理员确认 → 追加 level_audit_result 事件

**阶段 E（销售域联动）**：
- `LeadDetail360.tsx`：基础信息 Tab 新增 6 个派发字段（业务线/渠道计划/派发时间/派发目标/派发时效/首联时效）
- 五池列表（MyLeads/PublicLeads/AllLeads）：SLA 信息融入「跟进倒计时」列底部 mini Tag 显示

### 2. 项目阻塞项+风险等级

- `project-management/types.ts`：新增 ProjectBlocker/BlockerSource/BlockerSeverity/ProjectRiskLevel 类型
- `ProjectDetail360.tsx`：指标胶囊加风险+阻塞计数；档案卡加阻塞项列表+风险备注+验收标准
- `project-management/__tests__/projectBlockers.test.ts`：7 测试覆盖类型完整性

### 3. 真实项目 Mock 数据落地

- `project-management/projectMockData.ts`：追加 6 个真实项目（智能家居/智能酒店/中铁/小红书/汽车配件/重庆B端）+ 活动事件
- `contracts/mockData.ts`：追加 6 个合同（含完整回款计划+触发条件+回款记录）

### 4. β 版阶段 0：数据迁移脚本

- `scripts/migrate-mysql-to-d1.ts`：解析 MySQL INSERT → 映射 HubX 类型 → 输出 D1 SQL
- `apps/api/migrations/0005_seed_from_mysql.sql`：1882 线索 + 2 合同 + 54 项目 + 100 员工
- 字段映射：clue_type/source/level/customer_level/create_time/pro_status/pro_priority

### 5. β 版阶段 1：前端服务层接线

- `services/employeeService.ts`：createMockEmployeeService() + createHttpEmployeeService(baseUrl)
- `pages/employee/EmployeeContext.tsx`：接受 service prop，β 模式从后端加载员工
- `app/App.tsx`：新增 employeeService prop，注入 EmployeeProvider
- `apps/web/src/main.tsx`：注入 createHttpEmployeeService(apiBaseUrl)
- `apps/api/schema.sql`：新增 employees 表
- `apps/api/src/index.ts`：新增 GET /api/employees + GET /api/employees/:id

## 当前状态

| 项目 | 数据 |
|---|---|
| 工作区 | 大量未提交改动（本次编码+迁移+接线）；**未 commit / 未 push / 未部署 Cloudflare** |
| 主分支 HEAD | `4fdecb1` |
| α 本地服务 | `http://localhost:5173/` 后台运行中 |
| 测试基线 | 65 文件 886 测试全绿 |
| build | 通过（15s） |
| β 接线 | 6 个服务全部有 mock+HTTP 双实现，β 前端已注入 |

## 下次待办

1. **β 阶段 2**：后端 API 补全（GET /api/leads/:id/detail、GET /api/projects/:id/detail、线索派发域 API）
2. **β 阶段 3**：β 前端页面切换（所有页面使用真实数据渲染）
3. **β 阶段 4**：部署与验证（D1 迁移执行 + Workers 部署 + Pages 部署）
4. **提交发布**：本次改动量大，需 commit/push/部署 Cloudflare

## 踩坑

- MySQL INSERT 语句有 664KB 单行，正则 `(.+?)` 非贪婪匹配会截断，需用手动解析到非字符串分号
- speed_project 的字段顺序与 schema 定义不完全一致（id 在数据中但不在 CREATE TABLE 列定义中），需按实际 INSERT 顺序映射
- speed_contract_v2 和 speed_contract 是两张不同结构的表，需分别处理
- EmployeeContext 原本硬编码 mock，需加 service prop 支持 β 模式

## 文档联动

- 功能看板：线索派发 planned 状态需更新为「α 已实现」（阶段 0-B-C-D-E 完成）
- 工作记录：需追加 wl-2026-08-25-8~12（编码类五条）
- 架构图：无需改（线索派发/全局搜索/智能会议条目已在）
- 技术架构：需改（β 接线：employees 表 + employeeService + 6 服务全部 mock+HTTP）

# 2026-08-28 α 未确认项目唯一 ID 收口日志

## 收口范围

- 仅处理 α 原型，未改动 β Workers 持久化逻辑。
- 线索首次进入“合同洽谈/已签单”时生成未确认项目。
- 后续创建合同时绑定原项目，不生成重复项目。

## 统一规则

- 有线索：`ap-lead-{normalizeLeadIdentity(leadId)}`。
- 无线索合同：`ap-{contractId}`。
- `9` 与 `lead-9` 视为同一线索；合并时保留原项目 ID、状态与人员，只补齐合同关联及合同侧展示信息。

## 实现结果

- `LeadDetail360` 的“写跟进”已真实写入 `LeadService`，不再只更新页面内存。
- `SigningOpenBridge` 统一处理线索状态跃迁与合同创建。
- `SigningOpenBridge` 等待线索/合同 Provider 加载完成后再建首帧快照，刷新不再把历史数据误判为新事件。
- `ProjectService.create` 按项目 ID、线索别名和合同 ID 幂等查找并合并。
- 旧 `lead-spawn-*` 生成规则已从 α 源码退出。

## 验证结果

- 全量 Vitest：通过。
- `npm run build`：通过。
- 浏览器实测：干净刷新时无历史事件误通知；`/leads/5940?from=my` 登记“合同洽谈”后，项目列表由 14 条基线增至 15 条，仅有 1 条匹配的待确认项目，详情地址为 `/projects/ap-lead-5940`。

## 剩余工作

β 仍需在 B5/U1 将同一规则落到 Workers/D1，并验证刷新、并发重试和先洽谈后建合同场景不产生重复项目。

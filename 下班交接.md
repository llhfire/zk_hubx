# 下班交接

> 会话上下班交接文档（固定单份）。上班时先读此文件恢复上下文。
> **下班 = 写本文件 + 有代码改动才发布 Cloudflare**（用户说「下班」即触发，不要再确认）。发布步骤见 `HubX/docs/DEPLOYMENT.md`「下班发布」。
> 更新时间：2026-08-18（下班：U1 + 报价 4.1 全量编码完成）

## 本次进度（本次会话做了什么）

**U1（立项触发改回签约开启）+ 报价 4.1（状态机与词表收口）全部完成编码。**

### U1 完成项

1. **T1 `buildUnconfirmedProject`**：纯函数，contract 可选，两条接线共用。3 个测试。
2. **T2 `signingOpenEvents.diffContractEvents`**：首帧不触发，8 个测试（含同帧混合边界）。
3. **T3 桥改造**：`ApprovalDeliveryBridge` → `SigningOpenBridge`，消费 diff 事件——created→spawn，approved→startDelivery（无项目不兜底 spawn，ADR 0067）。
4. **T4 LeadDetail 跟进接线**：跟进 onOk 写本地 `leadStatus` state + 状态∈签约词表且无项目时 spawn 联动。头部展示改用本地 state。

### 4.1 完成项

1. **T1 词表收敛**：13 态→10 态（`pending_eval/pending_quote/confirmed` 合并六旧态）+ `migrateQuote` 读时迁移 + `TRANSITIONS` 合法迁移表 + `generateQuoteNoV2`（QT-YYYY-序号）。28 个测试。
2. **T2 quoteFlow 重写**：`deriveStage/getStageAccess/isTerminalStatus/getPendingOwner/getPendingRoles` 全按新词表 + `isExpired` 纯函数。测试全量改写。
3. **T3 动作层**：`mark_deal→mark_confirmed` + 新增 `withdraw_sent/return_to_stamp/return_to_edit_features` 三个回退动作。service/Context/Stage4 全链改名。
4. **T4 单号**：`QT-YYYY-序号` 格式，按前缀过滤取最大序号，每年从 1 开始。
5. **T5 种子+读路径**：mockData/api 种子写新词表 + mock/HTTP/D1 三处读路径统一过 `migrateQuote`（API 内联简化版）。
6. **T6 UI 文案+清零**：「确认成交/已确认/已废止/待审核/已驳回」+ grep 验证全库无旧词字面量。

**测试**：318 通过（+34 新用例），4 基线债不变，`npm run build` 绿。

## 当前状态要点

- 架构：单一 git 仓库；monorepo 在 `HubX/`
- 已通 HTTP：报价域、合同域；项目/商机/交付计划仍是内存 mock
- 部署：`alpha.zkhubx.com` / `beta.zkhubx.com` / `zkhubx-api.llhfire.workers.dev`，线上 SHA = `26792f1`（本次发布后更新）
- **销售域**：U1 代码在库（签约开启联动）；阶段 3 桥已改名 SigningOpenBridge；报价词表已收口 10 态
- **报价域**：4.1 全部落码（状态机 10 态 + migrateQuote + TRANSITIONS + 单号 QT 格式 + 4 个新回退动作）
- 本地 α：`http://localhost:5173/` 后台 Vite 应还在跑
- 工作区：8 份 dev-plan + 本次代码改动，待提交

## 下次待办（按优先级）

1. **人工验证 U1 §5.5**：7 条验证脚本（洽谈 spawn / 草稿 spawn / 批准=开工≠立项 / 批准不 spawn / 不重复 spawn / 已终止不 spawn / 全量测试+build）。
2. **人工验证 4.1 §5.7**：旧词清零 / 新回退链 / 成交改名确认成交 / 浏览器旧 localStorage 刷新后正常。
3. **按计划继续**：U3（作废↔搁置）或报价 4.2（列表/待我处理）。
4. 编码间隙顺手做薄计划：P0② 回款权限矩阵单测 / 基线债清偿 / 待办 #5。
5. 4 项未立项结构债登记进功能看板（客户域统一等需要先 grill）。

## 踩坑提醒

- **报错优先看计划**：开工前先读对应 dev-plan 的「边界决策」节。
- **已确认 / 签约 / 已签单** 三个词不要混。报价终态=已确认；线索终态=已签单；签约开启=洽谈/已签单或主合同出现。
- **Edit 工具对个别特殊字符（如 ≠）可能匹配失败**：换个不含特殊字符的锚点即可。
- 后台 Agent 偶发「恢复后不回传」：用 SendMessage 催两次仍无果就自己上手。
- 工具链 `${...}` 显示可能失真。判定用 `grep -c`。
- 会议录音不要 `git add`。改架构图只改根目录 `ZK-HubX架构图.html`。
- 改看板同步 `featureBoard.config.json` 与 `featureBoardModel.ts` 的 SEED；跑 `npx vitest run ../../packages/ui/src/app/version/__tests__/featureBoardModel.test.ts`（在 `HubX/apps/prototype`）。
- vitest 工作目录：`HubX/apps/prototype`
- **下班发布**：α/β 前端必发；`apps/api` 有改必发 Workers（本次有改）。
- **迁移函数 `migrateQuote` 是读时迁移**：旧 localStorage 数据刷新后自动转新词表，不主动写回。
- **U1 桥改名**：`ApprovalDeliveryBridge` 文件名保留（避免 git 丢失历史），但组件导出名改为 `SigningOpenBridge`，App.tsx 已更新。

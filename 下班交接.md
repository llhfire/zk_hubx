# 下班交接

> 会话上下班交接文档（固定单份）。上班时先读此文件恢复上下文。
> **下班 = 写本文件 + 发布到 Cloudflare**（用户说「下班」即触发，不要再确认）。发布步骤见 `HubX/docs/DEPLOYMENT.md`「下班发布」。
> 更新时间：2026-08-18（阶段 3 完成）

## 本次进度（本次会话做了什么）

- **新增「状态指示文档联动规则」并写入根 CLAUDE.md**：①做了计划就要更新 `ZK-HubX架构图.html`；②做了计划/完成模块编码要更新功能看板细则；③看板新增板块/功能要同步到架构图。本次已按规则执行（架构图 5 个模块条目、看板 4 个模块细则）。
- **销售域重构阶段 3 全部完成**（计划见 `.claude/plans/phase3-deal-to-delivery.md`，用户三项设计拍板：成交按钮跳向导预填 / 审批全通过触发 / 回款 Tab 写回）：
  1. **报价成交生成主合同**：Stage4 成交态「生成主合同」按钮 -> 合同向导预填（新纯函数 `contracts/dealQuotePrefill.ts`：金额/付款条款->期次/工期->终止日期/质保文案）-> 创建成功回写 `quote.contractId` + 维护商机 `quoteIds/contractId`。
  2. **合同批准触发交付启动**：新 `ApprovalDeliveryBridge.tsx`（无 UI 桥组件，挂在 App 最内层，监听合同 `approvedAt` 首次写入，首帧快照不误触发；对 mock/HTTP 数据源都生效）。无项目 -> `spawnUnconfirmedProject` 首次真正接线；未开始/搁置项目 -> `startDelivery`（新纯函数，caseUtils）转「进行中」+ `generateDeliveryPlan` 自动生成 SOP 计划写入新 `delivery-plan/deliveryPlanStore.ts`。
  3. **合同详情回款 Tab**：期次状态（已收/部分到账/逾期/即将到期/待收，`paymentUtils.computePlanStatusRows` 新纯函数，7 天缓冲与看板口径一致）+ 登记回款写回 `ContractsContext.addCollection`；补充协议登记弹窗改标题「登记回款（补充协议）」区分双入口。
  4. **新 BusinessCaseContext**（商机共享状态，App.tsx 挂载），向导回写与联动桥共用。
- **验证**：`npm run build` 通过；新增/更新 46 断言全绿（dealQuotePrefill 5 + startDelivery 5 + computePlanStatusRows 3 + 既有扩展）；全量测试与基线完全一致（4 断言失败 + react-quill 均旧债，未新增）。
- 本地 dev server 全程后台运行（http://localhost:5173）。**人工验证路径还没走**（报价成交->生成合同->审批通过->项目拉起+回款 Tab），建议下次上班先走一遍。

## 当前状态要点

- 架构：monorepo（packages/ui + apps/prototype α + apps/web β + apps/api Workers+D1）
- 已通 HTTP：报价域、合同域；项目域/商机/交付计划仍是内存 mock（ProjectContext / BusinessCaseContext / deliveryPlanStore，均已收口）
- 部署：`alpha.zkhubx.com` / `beta.zkhubx.com` / `zkhubx-api.llhfire.workers.dev`
- 销售域重构：阶段 0-2 见 git（23d3be9 / 619598a）；**阶段 3 本次完成（见本次提交）；阶段 4 未做**
- 本次已提交 git 并 push GitHub 备份；**未发布 Cloudflare**（用户未说「下班」，下次下班一并发布）

## 下次待办（按优先级）

1. **人工验证阶段 3 链路**：报价中心 -> Stage4 成交 -> 生成主合同 -> 提交审批逐节点通过 -> 项目自动拉起/交付启动 -> 回款 Tab 登记。
2. **阶段 4**：合同标的视角 + 多份补充报价/补充合同列表（BusinessCase.extraContractIds/quoteIds 已建模，quoteIds 已开始维护）。
3. 售前跟进 mock（ProjectPresalesHistoryPanel.PRESALES_FOLLOW_MOCK）与线索跟进记录收成同一份（阶段 3 挪到阶段 4 一起做）。
4. 阶段 3 后续可选项：交付类型默认「网站」（可从报价 endpointConfigs 推导）；无 leadId 手动合同的交付启动入口；β版后端复合 endpoint（成交生成合同/批准生成项目的跨表事务，目前联动全在前端 Context 层）。
5. 旧债（可后置）：P0② 回款权限矩阵单测；测试债（4 断言失败 + react-quill jsdom 文件级失败）。
6. 根 CLAUDE.md「仓库结构」一节仍写着 HubX/ 是独立嵌套仓库，**已过时**（实为单一仓库），顺手修正。

## 踩坑提醒

- **工具链 `${...}` 显示失真（本次澄清）**：工具**结果传输**会把模板字符串插值显示成占位符，探索代理因此误报了 contractService/quotationService getOne 的「模板串 bug」——python 字节级验证磁盘完全正常。**判定真伪用 `grep -c 'glm' 文件`（磁盘无此串=没问题），写码后照旧 grep 一次**。写入侧的吞插值风险仍未排除，继续用字符串拼接写关键 URL。
- vitest 在 `HubX/apps/prototype` 下跑：`npx vitest run ../../packages/ui/src/app/business-case/__tests__/caseUtils.test.ts`
- 全量测试基线：4 个断言失败（workAttribution×2 / projectTasks×1 / dailyReportModalInit×1）+ react-quill jsdom 2 个文件级 FAIL，均为既有债
- 基线对比技巧：`git worktree add /tmp/xxx HEAD` + 软链 node_modules，可快速验证失败是否为既有
- 签约触发：线索状态「合同洽谈」/「已签单」，或已有未作废合同；**spawn 现在挂在合同 approvedAt 首写上**（ApprovalDeliveryBridge）
- 项目 `未确认` 对产品经理默认不可见；管理员可见并确认
- `featureBoard.config.json` 与 `featureBoardModel.ts` 的 `PLANNED_SEED` 需同步；改看板用 python json 脚本后跑 featureBoardModel 测试校验
- 根目录就是 git 根（`HubX/` 不再是独立嵌套仓库）；`alphaChecklist.config.json` 已废弃
- 下班发布：α/β 前端必发，apps/api 有改才发 Workers

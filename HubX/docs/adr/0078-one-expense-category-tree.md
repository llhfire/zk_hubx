# 全公司只保留一套费用科目，可录入八个一级，LABOR 不是录入科目

报销、差旅、线索成本、运营费用台账必须对同一棵树。沿用已有 `TRAVEL` / `PROMOTION` / `BUSINESS` / `THIRD_PARTY`，新增办公、福利、人资行政、其他。策划案里的 `MARKETING` / `IT_SERVICE` 会与现有码分叉；`LABOR` 已是项目成本桶（工时×时薪），不能再拿来录工资或招聘。税费与财务成本本阶段不建科目，避免有人录入后再争论要不要进公摊池。
